# tasksAreUs — Product Requirements

## Overview

tasksAreUs is a task management app with a Python/FastAPI/PostgreSQL backend, a React 19 + Vite web frontend, and a React Native + Expo mobile client. Users manage tasks through a kanban board. The app is multi-tenant from day one. Identity is Firebase-only: the backend auto-creates a user row on first authenticated request; no device registration or migration step is needed.

---

## Boards

Each user can have up to **10 boards** (server-side cap: `MAX_BOARDS_PER_USER = 10`). Boards are the top-level organizational unit — tasks, labels, and conversations each belong to exactly one board.

**Default board**: Every user has exactly one default board. For new users it is created automatically on first access (any endpoint hit) and named **"General tasks"**. Existing users received a "General tasks" board via startup migration (PR #33) — all their existing tasks, labels, and conversations were assigned to it. **As of PR #62**, the default board is order-derived: it is always whichever board is topmost in the user's custom board order (set by drag-to-reorder in web Settings), and the server automatically re-promotes the new topmost board to default whenever the order changes. `is_default` is no longer a directly client-settable field.

**Board isolation**: Labels and tasks are strictly board-scoped. A label from Board A cannot be applied to a task in Board B. The AI (both belief generation and conversational interface) only sees tasks and labels belonging to the conversation's board.

**Label seeding**: When a new board is created for a new user, type labels (5 entries total) are seeded into that board. Label uniqueness is enforced per board: `(board_id, category, value)` — not per user.

**Backward compatibility**: All pre-existing endpoints accept requests that omit `board_id`. The server resolves the missing value to the user's default board, so old mobile and web clients continue to work without changes.

### Board API (shipped — PR #33; custom order + order-driven default — PR #62)

- `GET /api/v1/boards` — list the current user's non-deleted boards, ordered by the user's custom drag-to-reorder position (`sort_order` ascending, then creation time as a tiebreak) — updated PR #62 (previously default-first then creation time). Returns a `boards` array, each item now including `sort_order: float`. As of PR #37, `ensure_board_seeded()` is called at the top of this endpoint — a brand-new user gets their default "General tasks" board and 9 default labels created on the first `GET /boards` call (previously seeding was only triggered by task/label endpoints via `resolve_board_id`).
- `POST /api/v1/boards` — create a new board for the current user. `422` if the 10-board cap would be exceeded. `400` if the name is empty. New boards are placed at the bottom of the custom order.
- `PUT /api/v1/boards/{id}` — rename a board, reposition it in the custom order, and/or set/clear its color. **Updated PR #62**: `is_default` is no longer a directly writable field — the default board is server-derived from board order instead. The request now accepts `sort_order` (a client-computed float, set via drag-and-drop in web Settings); whenever a board's `sort_order` changes, the server recomputes which board is topmost across the user's full board list and automatically promotes it to default, demoting the previous default in the same transaction — dragging a board to the top of the list is what makes it the new default. A stale client that still sends `is_default` gets a normal `200` with the field silently ignored (Pydantic drops unknown fields). The `color` field accepts a 7-character hex string (e.g. `#6366f1`) or `null` to clear; omitting `color` entirely leaves the existing value unchanged (sentinel logic via `model_fields_set`). `BoardOut` now includes `color: Optional[str]` and `sort_order: float`.
- `DELETE /api/v1/boards/{id}` — soft-delete a board. Returns `400` if it is the only board, the default board, or still has tasks or labels.

### Board UI — Web frontend (shipped — PR #34)

- **Board switcher — removed, replaced by board tabs (PR #41)**: The always-visible `BoardSwitcher.tsx` dropdown (previously embedded in the desktop sidebar below the "tasksAreUs" title, and in the mobile web header) was deleted. Board switching on the web is now done via **board tabs** on the Tasks page, visible only when the All view is selected (see "Task List / Kanban Board"). This was an explicit, user-confirmed tradeoff at the time: re-scoping Settings' board-scoped Labels editor to a different board required Tasks → All → pick a board tab → Settings, rather than switching from any page via the old always-visible dropdown. **This tradeoff was superseded in PR #42**: Settings' Labels section now has its own independent board selector (see "Settings Page" below), so it no longer depends on the board tabs at all.
- **Create board inline**: Still available from the Settings → Boards section via a "+ Add board" button (inline text input; submitting creates the board via `POST /boards` and immediately activates it; a `422` (board cap reached) or `400` (empty name) error is shown inline). The switcher-dropdown "New board" option described in earlier PRs no longer exists, since the switcher itself was removed in PR #41.
- **Active board state**: Tracked in `BoardContext`. Initialised to the user's default board on every app open — never persisted to localStorage. Switching boards clears any active label filters (labels from Board A are not valid in Board B).
- **Board-scoped data**: All data fetches (tasks, reports) are scoped to the active board by passing `board_id` to API calls. Hooks (`useTasks`) and pages (`TaskDetailPage`, `ArchivePage`) re-fetch automatically when the active board changes. (`ChatPage`'s board-scoping is moot on web as of PR #41 — see the Chat Page aspirational note above.) **As of PR #42, Settings' Labels editor is the one exception** — it fetches labels for a board chosen via its own independent selector, not the active board (see "Settings Page" below); `useLabels` itself still defaults to the active board for other callers (e.g. the All-view kanban board's label filter chips).
- **Archive re-fetches on its own board-tab/date-range changes (updated PR #63)**: The Archive page no longer scopes itself to the app's global active board — it re-fetches automatically whenever its own local board-tab selection or date range changes (see "Archive" section below).
- **Settings — Boards section**: A new "Boards" section appears at the top of the Settings page (above Labels). Boards are listed in the user's custom drag-to-reorder order; each row can be dragged to reposition it, and the topmost board is the default (shown via a read-only star, no click action — updated PR #62; see "Settings Page" below for detail). Delete is disabled for the default board and for the only remaining board. Adding a new board is available via a "+ Add board" button. As of PR #42, the Labels section below it is board-neutral — see "Settings Page" under Frontend for detail.

### Board UI — Mobile app (shipped — PR #35)

- **BoardContext**: `BoardProvider` + `useBoard()` added to the mobile app, mounted inside `AppNavigator`'s authenticated branch (wrapping the `Tab.Navigator`). Fetches boards via `GET /boards` once on mount and exposes `activeBoard`, `boards`, `setActiveBoard`, `createBoard`, `renameBoard`, `deleteBoard`, `refetch`, and `setColorBoard` (calls `updateBoard(id, { color })` then `fetchBoards()` — added in PR #38). **`setDefaultBoard` was removed in PR #62** — mobile is read-only with respect to board order/default; boards render in whatever order `GET /boards` returns (the user's custom drag-to-reorder order, set from web Settings), and the default board is always the topmost one (server-derived — there is no mobile UI to change it). On fetch, `activeBoard` is preserved when the same board still exists after a mutation; otherwise it resets to the default board.
- **Board switcher in Tasks header**: The active board name is shown as a tappable title in the `TasksScreen` header. When the user has more than one board, a "▾" chevron is shown. Tapping opens an inline dropdown overlay (absolute-positioned below the header, `zIndex: 50`) listing all boards — a checkmark marks the active board and a star marks the default. Tapping a board calls `setActiveBoard()` and closes the dropdown.
- **Filter reset on board switch**: A `useEffect([activeBoard?.id])` in `TasksScreen` clears the label filter selection, text search query, and show-done toggle whenever the active board changes.
- **Board-scoped data fetches (mobile)**: All API calls now pass `activeBoard?.id` as an optional `boardId` parameter: `listTasks`, `createTask`, `listLabels`, `createLabel`, `createConversation`, and `getCompletions`. Each API helper accepts an optional `boardId` param appended to the query string or included in the request body.
- **Chat reset on board switch**: A `useEffect([activeBoard?.id])` in `ChatScreen` sets `conversationIdRef.current` to `null` and clears messages, so the next message creates a new conversation scoped to the newly active board.
- **Archive board scoping (updated PR #63)**: `ArchiveScreen` (renamed from `ReportsScreen`) no longer passes the shared `activeBoard?.id` to `getCompletions` — it scopes requests via its own local board-tab selection instead (see "Archive Screen" section below); the `getCompletions` helper accepts optional `boardId`/`allBoards` options.
- **Settings — Boards section (mobile, updated PR #62)**: A `BoardSection` card appears at the top of `SettingsScreen` (above Labels). It shows a board count badge (`N/10`), lists all boards — in the user's custom order, as returned by `GET /boards` — with a read-only default-star indicator, inline rename, and delete actions. The set-default tap action was removed in PR #62 (the default is now order-derived; mobile has no drag-and-drop UI, so the default can only be changed from web Settings). Delete is guarded: the UI shows the Delete button only when more than one board exists; backend errors (e.g. board has tasks) are surfaced as inline error text. A "+ Add board" inline input (capped at 10 boards) allows creating new boards. The Labels section fetches labels scoped to the active board via `listLabels('mode', activeBoard?.id)` and `listLabels('type', activeBoard?.id)`. New labels created in Settings are created in the active board.
- **Types updated**: `Board` interface added to `mobile/src/types/index.ts`. `Task.board_id: string` and `Conversation.board_id: string` added to match the API response shape. `color: string | null` added to the `Board` interface (PR #38). `sort_order: number` added to the `Board` interface (PR #62, documentation-only — mobile reads but never writes this field).
- **Tests**: 7 new Jest unit tests for the boards API (`mobile/src/__tests__/boards.api.test.ts`); `makeTask` factory updated in 3 existing test files to include `board_id: 'board-1'`. Total: 77 mobile unit tests pass (86 as of PR #38).

---

## Focused View

The Focused View shows only high-priority tasks from a user-configured set of boards and day range, presented as a card grid grouped by board. On the web frontend it is one of four session-only views (Focused, Today, Tomorrow, All) selectable via a pill toggle in the Tasks page header — Focused is the default view on every page load (shipped — PR #41; see "Task List / Kanban Board").

### Behavior

- Shows tasks where `priority = 'high'` AND (`must_do_by` OR `target_date`) falls within the configured day range.
- **Priority display note (PR #73)**: only High-priority tasks are shown; Medium-priority tasks are filtered out (even though Medium is valid on dates within Focused View's date window). This is an intentional product decision — Focused View is expressly for high-priority tasks only. Medium-priority tasks are visible in the All view's kanban board, the Today/Tomorrow views, and the Overdue view.
- Overdue tasks are **not** shown — the Focused View is forward-looking only. Overdue tasks remain visible in the kanban board (the web All view; the mobile Detailed view).
- Board groups are ordered by the user's custom board order (updated — PR #62; previously alphabetical by board name) — the same drag-to-reorder position set in web Settings' Boards section, which also determines the default board. Within each board group, tasks are sorted by `sort_order` ascending (updated — PR #61; previously `updated_at` descending) — the manual position set by drag-to-reorder on the All view's kanban board, reset to the bottom of its zone whenever the task's date or board changes, or (for tasks that predate this feature) backfilled from `created_at`. The Focused View has no reorder UI of its own for either boards or tasks; it simply reflects whatever order was last set elsewhere.
- Boards with zero qualifying tasks are silently omitted from the response.
- Each board group is color-coordinated using the user-configurable `boards.color` hex value (null = UI assigns a default from a fixed palette).
- Cards are tap-to-open only — no drag-and-drop.

### Data Model (shipped — PR #36)

**New table: `focused_view_configs`** — one row per user, enforced by `UNIQUE(user_id)`.

| Column | Type | Notes |
|---|---|---|
| `id` | UUID | PK |
| `user_id` | UUID | FK → users (CASCADE), indexed, UNIQUE |
| `board_selection` | VARCHAR | `all` or `selected` |
| `selected_board_ids` | JSONB | Array of board UUID strings; ignored when `board_selection = all` |
| `day_range` | VARCHAR | `today`, `today_tomorrow`, or `today_plus_two` |
| `created_at` | TIMESTAMP | |
| `updated_at` | TIMESTAMP | |

Default config (created lazily on first GET, upsert-safe): `board_selection = all`, `day_range = today_tomorrow`, `selected_board_ids = []`.

**Modified table: `boards`** — `color VARCHAR(7)` column added via startup migration `ALTER TABLE boards ADD COLUMN IF NOT EXISTS color VARCHAR(7)`.

### Focused View API (shipped — PR #36, backend only; default changed in PR #40)

- `GET /api/v1/focused-view/config` — returns the user's config, creating the default lazily via upsert if none exists. **Default config (PR #40 change)**: `board_selection = all`, `day_range = "today"` (changed from `"today_tomorrow"` in PR #40; existing users migrated via one-off script), `selected_board_ids = []`.
- `PUT /api/v1/focused-view/config` — updates `board_selection`, `day_range`, and optionally `selected_board_ids`. Validates: `board_selection` must be `all` or `selected`; `day_range` must be one of the three valid values; when `board_selection = selected`, `selected_board_ids` must be non-empty and all IDs must belong to the caller. Returns `400` for any violation.
- `GET /api/v1/focused-view/tasks` — returns `is_high_priority=true` pending tasks grouped by board, filtered to the configured day range. Accepts optional `?reference_date=YYYY-MM-DD` query parameter (avoids server-side timezone issues; defaults to `date.today()` when omitted). Response shape: `{ "boards": [{ "board_id", "board_name", "board_color", "tasks": [TaskOut, ...] }, ...] }`.

### Focused View UI — Web frontend (shipped — PR #37)

**Color picker in Board settings**: Each board row in Settings → Boards gets a color swatch (a circle showing the current hex color, gray if unset). Clicking it opens a hidden native `<input type="color">`. Changes auto-save with a 300ms debounce via `setColorBoard` in `BoardContext`, which calls `PUT /boards/{id}` with the hex value and re-fetches boards. A `×` button (visible only when a color is set) calls `setColorBoard(id, null)` to clear it. `BoardContext` now exposes `setColorBoard(id: string, color: string | null): Promise<void>` (analogous to `renameBoard`).

**Focused View config section in Settings — removed (PR #41)**: The "Focused View" section that appeared below Boards and above Labels (board selection radio, board checkboxes, day range radio, its own Save button calling `updateFocusedViewConfig`) was deleted from the web `SettingsPage.tsx` in PR #41. See "Settings Page" below for detail. The backend config endpoints are unaffected.

**View toggle in TasksPage header — superseded (PR #41)**: The original session-only "Detailed" | "Focused" pill toggle was replaced by a four-way **Focused | Today | Tomorrow | All** pill toggle (Focused still the default). The "Show done" concept no longer exists on this page, so there is nothing for the toggle to reset on. See "Task List / Kanban Board" above for full detail.

**FocusedView component** (`frontend/src/components/FocusedView.tsx`): Rendered in place of the kanban board when view mode is "focused" and pending mode is active. Fetches `GET /focused-view/tasks?reference_date=YYYY-MM-DD` on mount only (`useEffect(() => { ... }, [])`), using `dateOnly(new Date())` for the local-timezone date. Does not refetch on active-board change (focused view is config-driven server-side). Shows loading spinner while fetching; inline error banner with Retry button on failure; empty state ("No focused tasks for this period") when no qualifying tasks exist. Tasks displayed as a responsive card grid (`grid-cols-1 sm:grid-cols-2 lg:grid-cols-3`) grouped by board, each group headed by a color-coded row using `board_color` from the API or a fixed 8-color palette (`['#6366f1','#f59e0b','#10b981','#ef4444','#3b82f6','#8b5cf6','#ec4899','#14b8a6']`) indexed by board position.

**Board collapse/expand and pin (shipped — collapse PR #53, pin PR #69)**: Each board group has a collapsible header with a ▾/▸ chevron and a pin button (📌 emoji). Clicking the board header toggles collapse — when collapsed, the task grid is hidden but the header row (color dot, board name, task count) remains visible as a summary. Clicking the pin button on any board collapses every other board in the current view and expands only the pinned board. The first click on a pinned board's header unpins it without collapsing (revealing the previous manual collapse layout); a second click would then toggle collapse normally. "Collapse all / Expand all" clears any active pin and sets all boards to the specified state. Both collapse and pin states are session-only (in-memory, never persisted), scoped per-view (collapsing or pinning a board in Focused view does not affect its state in Today or Tomorrow), survive in-app navigation, and reset on a full page reload. All boards start expanded and unpinned on every fresh page load. If a pinned board is deleted or its tasks are rescheduled out of the current view's date window, the pin is automatically cleared and the previous manual collapse layout is restored.

**FocusedTaskCard component** (`frontend/src/components/FocusedTaskCard.tsx`, updated PR #81): Compact card with a 4px left colored border (`boardColor`) and a **background color determined by the task's date column** (PR #81): Overdue tasks render `bg-red-50` regardless of priority tier (making overdue status visually distinct); Upcoming and No Date columns render plain white (these columns cannot hold assigned priority tiers); all other date columns (Today, Tomorrow, Day After Tomorrow) render tier-colored backgrounds — light orange for High, light blue for Medium, light green for Normal (shipped — PR #79) — the background falls back to white while the card is in inline quick-edit mode. Displays: title (up to 2 lines, ellipsis), date chip using `getEffectiveDate` (red if overdue), label badges from `task.labels` (mode=green, type=purple; sorted alphabetically by value as of PR #70), and up to 3 links (shipped — PR #45; shown as their description, tapping opens the URL in a new tab). Click navigates to `/tasks/${task.id}` (unless in edit mode). No drag-and-drop. Labels are rendered directly from `task.labels` — no separate label prop needed. **Inline date editing (added PR #76)**: The effective-date badge is clickable. When tapped: if only one of `must_do_by`/`target_date` is set, a native date picker opens to edit that single field directly; if both are set, the badge expands to show independent "Must do" and "Target" links (matching the split-mode layout used on the All view) instead of guessing which field to change, avoiding the ambiguity of editing the earlier date and having the badge silently flip to show the later date. Edits commit immediately via a native `<input type="date">` with no separate Save button. **Priority stepper (added PR #78)**: Up/down chevron buttons at the top of the card replace the pencil icon, enabling direct priority adjustments without entering edit mode. The stepper behavior depends on the current priority: Normal tier shows single/double-up chevrons (→Medium/→High); Medium tier shows single-up/single-down (→High/→Normal); High tier shows single/double-down (→Medium/→Normal). On ineligible-date columns (Overdue, Upcoming), the stepper is hidden entirely. Upward shifts check date eligibility and demote to Normal if the target tier is ineligible; downward shifts never check (you already held an equal-or-higher tier, so stepping down never newly requests an elevated tier). The High tier is subject to the daily cap; attempting to exceed it shows an inline error and reverts the attempt. **Inline quick-edit (added PR #42)**: a pencil icon toggles an in-place editable form (title + label toggles) via the shared `TaskQuickEdit` component — the same quick-edit already available on All-view task cards. Complete and Delete buttons (added PR #45) are also available at the top of the card alongside the priority stepper (updated PR #78; previously in the bottom action row). Saving re-fetches the parent view (Focused or Day View, since this card is also used by Day View — see below) via an `onRefresh` callback.

**Label filter chips (updated PR #69, persistent per-board selection PR #75); text search extended to all views (PR #56)**: Label filter chips appear in two contexts: in the All view showing all boards' Type labels, and in the Overdue/Focused/Today/Tomorrow grouped views when exactly one board is visible (not collapsed, and has at least one task matching any other filters). In grouped views, chips are scoped to the visible board's labels only; toggling a label filters that board's task list without affecting other (collapsed) boards. As of PR #75, chip selections are now persistent by `board_id` across navigation and across views — the All view's board tabs and pinned/single-board views both read/write the same per-board memory, so a selection made on board-1 in the All view persists when viewing board-1 via a pinned view, and vice versa. Text search, however, filters tasks in every view (Overdue, Focused, Today, Tomorrow, All) as of PR #56 — see "Text search" under "Task List / Kanban Board" below.

**New frontend API module** (`frontend/src/api/focusedView.ts`): Exports `FocusedViewConfig` and `FocusedBoard` interfaces plus `getFocusedViewConfig()`, `updateFocusedViewConfig()`, and `getFocusedViewTasks(referenceDate?)`.

**New unit tests**: 2 new tests in `frontend/src/__tests__/boards.api.test.ts` covering `updateBoard` with a hex color and with `null` (clear).

### Focused View UI — Mobile app (shipped — PR #38, updated PR #46)

**Color swatches in Settings → Boards**: Each board row in Settings → Boards gets a horizontal row of 8 pre-defined color swatches (20×20 circles from a fixed 8-color palette: `['#6366f1','#f59e0b','#10b981','#ef4444','#3b82f6','#8b5cf6','#ec4899','#14b8a6']`) plus a "None" swatch (gray circle with ✕) at position 0. Tapping a swatch sets that board's color via `setColorBoard`; tapping the active swatch (or the "None" swatch) clears the color to `null`. The active swatch is highlighted with a border ring and shadow. No native color-picker module is required — the 8-swatch palette is OTA-deployable and uses the same palette as the web's `FocusedView.tsx` fallback.

**Focused View config section in SettingsScreen — removed (PR #46)**: The mobile Settings editor for Focused View config (board selection, day range) was removed as part of the Tasks-view redesign. The backend `GET`/`PUT /focused-view/config` endpoints remain live and unchanged — nothing in the mobile frontend calls them anymore.

**View mode toggle in TasksScreen header (updated PR #46)**: A four-button pill toggle (**Focused** | **Today** | **Tomorrow** | **All**) appears in the TasksScreen header. Focused is the default. The Collapse/Expand button and the filter (☰) button are visible only in All view. A `focusedViewKey` counter is incremented on each `useFocusEffect`, forcing FocusedView/DayView to remount and re-fetch fresh data on every tab re-focus.

**`FocusedView` component** (rendered when `viewMode === 'focused'`): Fetches `GET /focused-view/tasks?reference_date=YYYY-MM-DD` on mount using `dateOnly(new Date())` for local-timezone correctness. Shows a loading spinner while fetching; inline error banner with Retry on failure; empty state ("No focused tasks for this period") with a Refresh button when no qualifying tasks exist. Renders board sections in a `ScrollView` — tasks stacked vertically in a single column (appropriate for phone widths). Each board section header shows a colored dot (board color or palette fallback), board name, and task count badge. Board color comes from `board.board_color` or falls back to the 8-color palette indexed by board position.

**Board collapse/expand (shipped — PR #53)**: Each board header is tappable and shows a ▾/▸ chevron. Tapping toggles collapse — when collapsed, the task list is hidden but the header row (dot, name, count) remains visible as a summary. A "Collapse all / Expand all" button above the board list lets the user toggle all boards at once. Collapse state is session-only (in-memory, never persisted), scoped per-view (collapsing a board in Focused view does not affect its state in Today or Tomorrow), survives tab switches to Archive/Settings and back (the `TasksScreen` component is not unmounted by React Navigation's tab changes), and resets on a full app reload. All boards start expanded on every app launch.

**`DayView` component (new, PR #46, board collapse added PR #53)**: Fetches `GET /day-view/tasks?reference_date=YYYY-MM-DD` for Today or Tomorrow. Same structure and UX as FocusedView (loading/error/empty states, board-grouped rendering via shared `BoardGroupedTasks`), but shows all pending tasks (any priority) for the selected calendar day. Supports the same per-board collapse/expand with ▾/▸ chevron and "Collapse all / Expand all" button, with session-only per-view collapse state (independent for Today and Tomorrow, reset on app reload).

**`FocusedTaskCard` component (updated PR #46)**: Compact card with a 4px colored left border (`borderLeftColor: boardColor`). Displays: high-priority "★ High" badge (amber) when applicable, title, effective date chip using `getEffectiveDate`, label badges, and up to 3 link chips. Now uses a shared `TaskCardBody` component for its title/date/labels/links/actions rendering, with layout set to `'stacked'`. Inline quick-edit (added PR #42 on web, PR #46 on mobile) toggles an in-place `TaskQuickEdit` form (title + label toggles); tapping the Edit icon in the action row opens it. Complete and Delete buttons are also available in the action row (added PR #45 on web, PR #46 on mobile). Tapping anywhere on a non-editing card still opens `TaskFormScreen` in edit mode (unless `isEditing` is true, which suppresses the tap).

**`TaskCardBody` component (new, PR #46)**: A shared presentational component (RN port of the web component of the same name) used by both `TaskRow` (All view, inline layout) and `FocusedTaskCard` (Focused/Today/Tomorrow, stacked layout). Parameterized by `layout` (`'inline' | 'stacked'`), date-display mode (`'split'` or `'effective'`), priority-badge style (`'static'` — `'toggle'` is not used on mobile), and a labels render-prop. Owns Title/Date/Labels/Links/priority-indicator/Edit/Complete/Delete rendering, gated by `task.state === 'pending'` for the action row.

**`BoardGroupedTasks` component (new, PR #46)**: A shared rendering component for both FocusedView and DayView. Iterates over board groups and renders each via `FocusedTaskCard`.

**`TaskQuickEdit` component (new, PR #46)**: RN port of the web quick-edit component. Shown inline when `isEditing` is true on `FocusedTaskCard`. Renders: title `TextInput`, label chips grouped by category (Mode/Type only) with toggle interaction, and Save/Cancel row. Accepts an optional `labels` prop (board-scoped labels already on hand); when omitted, fetches via `listLabels(undefined, task.board_id)` — needed because FocusedTaskCard/DayView group tasks across boards.

**New mobile API module** (`mobile/src/api/focusedView.ts`, updated PR #46): Exports `FocusedBoard` interface (still used by both Focused View and Day View) plus `getFocusedViewTasks(referenceDate?)`. Config functions removed.

**New mobile API module** (`mobile/src/api/dayView.ts`, new PR #46): Exports `getDayViewTasks(referenceDate)` to fetch Day View tasks.

**Tests**: Existing `mobile/src/__tests__/focusedView.api.test.ts` refactored to cover only `getFocusedViewTasks` (config functions removed). New `mobile/src/__tests__/dayView.api.test.ts` with tests for `getDayViewTasks`. Deleted `mobile/src/__tests__/chatUtils.test.ts`. Total: 84 mobile unit tests pass (all 8 suites passing).

**Deployment**: OTA only — all JS/TS changes, no native modules, no `app.json` or `eas.json` changes.

---

## Day View

The Day View shows all pending tasks (any priority, not just high-priority) across all boards for a specific date or date range. It is used as the backing data for Today, Tomorrow, and Overdue views. Unlike Focused View, there is no configuration — Day View simply returns tasks matching the caller's date filter.

### Data Model

Day View uses the same `focused_view_configs` table for its future per-view configuration (when views expand beyond just Focused and Day). Currently, only Focused View configuration is stored.

### Day View API (shipped — PR #40, Overdue param added PR #55)

- `GET /api/v1/day-view/tasks?reference_date=YYYY-MM-DD` — returns all pending tasks (any `priority` value: high, medium, or normal) matching a date filter, grouped by board (same response shape as Focused View: `{ "boards": [{ "board_id", "board_name", "board_color", "tasks": [TaskOut, ...] }, ...] }`). Boards are ordered by the user's custom board order (updated — PR #62; previously alphabetical by name) — the same drag-to-reorder position used by Focused View and Settings' Boards section; within each board, tasks are sorted by `sort_order` ascending for Today/Tomorrow (`overdue=false`) (updated — PR #61; previously `updated_at` descending) — the same drag-to-reorder position used by the All view's kanban board. The Overdue view (`overdue=true`) keeps the original `updated_at` descending tiebreak, since Overdue tasks aren't reorderable and most-recently-touched-first remains the more useful default there. Boards with zero qualifying tasks are omitted. The `reference_date` query parameter is required (no default); omitting it returns `422`.
  - When `overdue=false` (default) — returns tasks with `must_do_by` or `target_date` equal to `reference_date` (used by Today and Tomorrow views).
  - When `overdue=true` (PR #55) — returns tasks whose effective date (earliest of `must_do_by`/`target_date`) is strictly before `reference_date` (used by Overdue view).

### Day View UI — Web frontend (shipped — PR #41, board collapse added PR #53, Overdue view added PR #55)

The Tasks page's **Today**, **Tomorrow**, and **Overdue** views render the Day View. Today and Tomorrow fetch `GET /day-view/tasks?reference_date=` for the respective date (with `overdue=false`, the default); Overdue fetches `GET /day-view/tasks?reference_date=today&overdue=true`. All three render results using the same board-grouped card layout as Focused View (`BoardGroupedTasks.tsx`, extracted from `FocusedView.tsx` and shared by all day-based views), including the same `FocusedTaskCard` cards — so the inline quick-edit added in PR #42 is available here too. Loading spinner while fetching; inline error banner with Retry on failure; empty state (e.g., "No overdue tasks", "No tasks for this period") with a Refresh button when no qualifying tasks exist. Day View is unconfigured — it always shows all-priority, all-board tasks for the specified date filter.

Each board group uses the same collapsible header design as Focused View (▾/▸ chevron, pin button, "Collapse all / Expand all" button), with session-only per-view collapse and pin state (independent for Today, Tomorrow, and Overdue; reset on page reload). When exactly one board is visible in a view (due to collapse or pin state), label filter chips appear between the "Collapse all / Expand all" row and the board list, scoped to that board's labels and filtering its tasks only — see "Label filter chips" above for behavior.


---

## Task Model

### Core Fields
- **Title**: short description of the task
- **Notes**: optional longer description
- **Must-do-by date**: hard deadline
- **Target date**: aspirational completion date (e.g. "I want to return library books by Friday, but they're due Sunday")
- **State**: Pending or Done
- **Priority tiers (shipped — PR #72)**: A three-level `priority` field (`high` / `medium` / `normal`) replaces the former boolean `is_high_priority`. High and Medium are only meaningful for tasks whose effective date (earliest of `must_do_by` and `target_date`) falls on today, tomorrow, day-after-tomorrow, or Monday (if today is Friday) — the same eligibility window as the old high-priority-only rule. Normal priority has no date restriction, same as today's non-high tasks. **`getEffectiveDate` correctly returns the minimum of the two dates when both are set (fixed in PR #20 for both web and mobile clients).** The backend auto-resets `priority` to `normal` whenever the effective date falls outside the eligibility window for High/Medium — **the backend's eligibility check itself only recognized up to tomorrow until PR #60**, so tasks dragged into the Day After Tomorrow or Friday-only Monday high-priority zones on the web kanban board had `is_high_priority` silently reset to `false` on save; the web Task Details checkbox had the same narrower cutoff and never appeared for tasks dated Day After Tomorrow or Monday. Both are fixed as of PR #60 to match the window described here. **Daily cap**: At most **N high-priority tasks** are allowed per day (where N is the user-configured **High Priority Daily Limit**, defaulting to 3, minimum 1); the API returns 422 when the limit would be exceeded. Medium and Normal priorities have no daily limit. Each calendar day (Today, Tomorrow, Day After Tomorrow, Monday) has its own independent limit enforcement as of PR #54. **Backward compatibility (PR #72)**: `is_high_priority` remains in the database and API responses as a mirrored field (`is_high_priority = priority == 'high'`) to support old mobile clients during the OTA update window — clients that haven't picked up the priority-tiers OTA update yet continue to read/write the boolean field unchanged. Old clients writing `is_high_priority` to an existing Medium-priority task do not demote it to Normal; the medium-preserving field-resolution rule ensures the legacy field cannot silently clobber Medium via an unrelated edit (e.g., editing the task's title) on an un-updated device.

### Links (shipped — PR #39, auto-ready rows and open-in-new-tab PR #52)

A task can have up to **3 links**, each with a required URL and a required short description (up to 200 characters). A URL must start with `http://` or `https://` (up to 2048 characters) — other schemes (e.g. `javascript:`, `data:`, `mailto:`) are rejected, both by client-side validation before submit and by the API.

- **Task Form**: A blank, ready-to-fill row always appears at the end of the links list while under the 3-link cap. When the user fills in both the URL and description for that row, a new blank row automatically appends (if the cap allows). There is no "+ Add link" button — the form handles row appending automatically. Each row can be removed independently. Saving is blocked with an inline error if a row has only one of URL/description filled in, or if a URL doesn't start with `http://`/`https://`; fully-blank rows are silently skipped. Saving a task always sends the full current link list to the API — removing all links and saving clears them. Each link with a valid URL displays an open-in-new-tab icon (web: SVG icon on hover, mobile: "↗" text) that opens the link in a new tab/app without navigating away from the form.
- **Task cards / rows** show each link by its description, not the raw URL. On web, hovering over a link reveals the full URL via a native tooltip and clicking opens it in a new tab. On mobile, tapping a link chip opens the URL directly — there is no hover step on touch, so the description is the only text shown.
- Links round-trip through offline sync on both platforms.

### Move Task Between Boards (shipped — PR #40)

A task can be moved from one board to another via `PUT /tasks/{id}` by providing a `board_id` field. **When a task is moved to a different board, its labels are unconditionally cleared** — this is necessary because labels are strictly board-scoped (a label from Board A has no meaning in Board B). The `board_id` field uses full-replace semantics: omitting it leaves the task's board unchanged (matching the behavior of `label_ids` and `links`); providing any value — including the current board — triggers the move logic. When `board_id` equals the task's current board, no change occurs and labels are preserved.

- **Label clearing behavior**: When a task moves to a new board, all labels are deleted server-side regardless of whether `label_ids` is included in the same request. If `label_ids` is also provided in the request, they are resolved against the **destination board's** labels (not the origin board). If none of the provided label IDs exist on the destination board, the request returns `422`.
- **API**: `PUT /tasks/{id}` with body `{ "board_id": "new-board-uuid" }` (and optionally other fields).
- **Sync**: Tasks pushed to `/sync` with a `board_id` that differs from their current board undergo the same move + label-clear logic.
- **Backward compat**: Stale clients that omit `board_id` in sync payloads continue to work — tasks stay on their current board.

### Labels
A task can have zero or more labels in fixed system-defined categories. A task may have **multiple labels within the same category** (e.g. both `online` and `email` under Mode).

| Category | Values | Editable? | UI Visible? |
|---|---|---|---|
| Frequency | one-time, daily, weekly, monthly, annual | No — Frequency is no longer a valid category in the API (PR #31); all DB rows deleted by SQL migration | **No** — hidden from all UI surfaces as of PR #29; no longer seeded as of PR #30; all DB rows deleted and enum value dropped as of PR #31 |
| Mode | online, phone, outdoor, email | No — Mode is no longer a valid category as of PR #51; all Mode labels deleted from database by startup migration; cannot be created via API (400 error) | **No** — Mode labels deleted from all boards (PR #51); no longer seeded or user-editable |
| Type | household, financial, child, trip, medical (defaults) | Yes — per-user; users can add, rename, and delete | Yes |

**Label ownership rules (shipped — PR #15, corrected in PR #16, updated PR #30, updated PR #31, updated PR #33, updated PR #51):**
- **All labels are per-board** — `labels.user_id` and `labels.board_id` are both `NOT NULL`. There are no global seed rows in the DB. A user's default board is seeded with **Type labels only** (5 entries total) on first access (as of PR #33, seeding is triggered by `resolve_board_id`/`ensure_board_seeded`; as of PR #37, also triggered by `GET /boards` directly — so the web frontend's `BoardContext`, which calls `GET /boards` on mount before any task or label endpoint, now creates the default board and labels for new users).
- **Label uniqueness is per-board**: the constraint is `(board_id, category, value)` — the same label value can exist independently in two different boards belonging to the same user.
- **Frequency** labels have been fully removed (PR #31): all frequency label rows were deleted from the DB by the SQL migration, `CategoryEnum.frequency` was removed from Python, and `GET /labels?category=frequency` now returns 400 (unknown category). Frequency labels cannot be created, edited, or deleted via the API and are not shown in Settings.
- **Mode** labels have been fully removed (PR #51): all Mode label rows were deleted from the database by a startup migration, the PostgreSQL `categoryenum` type was updated to drop the `'mode'` value, and `GET /labels?category=mode` / `POST /labels` with `category=mode` now return 400 (unknown/non-configurable category). Mode labels cannot be created, edited, or deleted via the API and are not shown in Settings.
- **Type** labels are per-user, per-board, and fully configurable: users can add, rename, and delete them. This is the only remaining configurable label category.
- Users cannot create or modify label *categories* — the one active category (Type) is fixed.
- The default Type value was renamed from `raghav` to `child` (applied via startup migration in PR #15).
- Seeding is driven by the `LABEL_SEED` constant in `models.py`; the `seed_board_labels()` helper lives in `services/label_service.py`. The seeding sentinel checks for any non-deleted boards (zero boards = new user) via `ensure_board_seeded()` in `services/board_service.py`.

**Frequency label removal — fully complete (PR #29 + PR #30 + PR #31 + PR #32):**
- `frequency` has been removed from all TypeScript `Label.category` unions in the frontend (`frontend/src/api/tasks.ts`) and mobile (`mobile/src/types/index.ts`) (PR #29 for UI surfaces, PR #32 for the `Label` interface itself).
- All label filter panels, label pickers (TaskForm / TaskFormScreen), label ordering arrays, color maps, and grouped-state initializers on both web and mobile no longer include `frequency` (PR #29).
- Backend recurrence logic removed (PR #30): `FREQUENCY_VALUES`, `_get_frequency_label()`, and `_next_due_date()` deleted from `task_service.py`; frequency labels no longer seeded for new users.
- **DB migration complete (PR #31)**: All frequency label rows deleted from the `labels` table (beliefs FK references nulled; `task_labels` join rows deleted). `frequency` dropped from the Postgres `categoryenum` enum type. `CategoryEnum.frequency` removed from Python. `GET /labels?category=frequency` returns 400 (unknown category). `POST /labels` with `category=frequency` returns 400. No frequency data remains in the DB.
- **Type cleanup complete (PR #32)**: `recurrence_group_id` removed from the `Task` TypeScript interface in both frontend (`frontend/src/api/tasks.ts`) and mobile (`mobile/src/types/index.ts`). The dead `.filter((l) => l.category !== 'frequency')` guard removed from `TaskCard.tsx`. Test fixture factories updated accordingly. `Label.category` union is now `'mode' | 'type'` throughout the TypeScript codebase.

**New label management API endpoints (shipped — PR #15, updated PR #31, updated PR #33, updated PR #51):**
- `POST /api/v1/labels` — create a new Type label for the current user in the specified board (or the default board if `board_id` is omitted). `400` if unknown/non-configurable category (Mode and Frequency categories are not configurable as of PR #51). `409` if a duplicate exists within that board. Strips whitespace.
- `PUT /api/v1/labels/{id}` — rename a Type label owned by the current user (403 if wrong owner, 400 if non-configurable category, 409 if duplicate name within the same board). Mode labels cannot be renamed (they were deleted in PR #51).
- `DELETE /api/v1/labels/{id}` — delete a Type label owned by the current user (403 if wrong owner, 400 if non-configurable category). Mode labels cannot be deleted (they were deleted in PR #51).

### Task Lifecycle

All tasks follow a single lifecycle: Pending ↔ Done (can transition in both directions). Completing a task via `POST /tasks/{id}/complete` always returns `next_task: null` (shipped — PR #30). Reopening a completed task via `POST /tasks/{task_id}/reopen` (shipped — PR #64) returns a `TaskOut` with the task's state reset to pending.

**Reopen task endpoint (shipped — PR #64):** `POST /tasks/{task_id}/reopen` transitions a completed task back to pending state. Returns `422` if the task is not in the done state. On success, sets `state = pending`, clears `completed_at`, resets `sort_order` to the default bottom-of-list position (via `_sort_order_default()`), leaves `priority` (and its legacy mirror field `is_high_priority`) unchanged at its current value, bumps `updated_at`, and returns the updated `TaskOut`. The endpoint validates task ownership via `get_task_or_404(user_id, task_id)` before proceeding.

**Recurrence logic removed (PR #30):** The backend no longer creates follow-on tasks when a task is marked done. `FREQUENCY_VALUES`, `_get_frequency_label()`, and `_next_due_date()` have been deleted from `task_service.py`. This makes recurrence behavior consistent with the UI-level removal of the Frequency label category (PR #29).

**DB migration complete (PR #31):** All frequency label rows have been deleted from the DB, `CategoryEnum.frequency` removed from Python, and `frequency` dropped from the Postgres enum. The `recurrence_group_id` column has also been dropped from the `tasks` table — it is absent from `TaskOut` schema, `GET /tasks` responses, `POST /sync` push and pull payloads, and the `GET /tasks` query parameter list. Stale mobile clients that still send `recurrence_group_id` in sync payloads are handled gracefully (the field is silently ignored).

**Board column added (PR #33):** `board_id` is now included in all `TaskOut` responses (`GET /tasks`, `POST /tasks`, `PUT /tasks/{id}`, etc.) and in sync pull payloads. `POST /tasks` and sync push accept an optional `board_id`; omitting it defaults to the user's default board. The sync pull response now includes a `boards` array containing any boards updated since `last_synced_at`. Stale clients that omit `board_id` in sync push task objects have their tasks automatically assigned to the user's default board.

**Type cleanup complete (PR #32):** `recurrence_group_id` has been removed from the `Task` TypeScript interface in both frontend and mobile clients, matching the backend schema. `Label.category` is now narrowed to `'mode' | 'type'` (the `'frequency'` variant has been removed from all TypeScript type declarations).

---

## Frontend

- React 19 + Vite + TypeScript + Tailwind CSS; runs in the browser
- `npm run dev` for local development (Vite dev server proxies `/api` to `http://localhost:8000`)
- **Browser page title (PR #70)**: The browser tab title reads "Tasks are us" (changed from "frontend" in PR #70)

### Pages / Tabs

1. **Tasks** (default `/`) — four session-only views selectable via a pill toggle, in this order: **Focused** (default), **Today**, **Tomorrow**, **All** (the kanban board). Current view is tracked in a session-only `ViewContext` and the active board in `BoardContext`; both are also encoded in the URL (`?view=&board=`). Because the contexts sit above the router, the selected view and board now survive navigating away to **Archive, Settings, or Task Detail and back** — not just Task Detail as before — while the URL encoding still restores the correct view/board on a hard reload or a shared/bookmarked link (shipped — PR #41; sticky cross-page state added PR #42). There is no "Show done" toggle on this page and no way to browse completed tasks from it (see "Task List / Kanban Board" below for full detail).
2. **Archive** (`/archive`, renamed from "Reports" at `/reports` — PR #63; the old `/reports` URL redirects to `/archive`) — completed tasks for a user-chosen date range. Alongside the existing manual From/To inputs, three quick presets (This month, Last month, Last three months — a rolling window from the 1st of the month two months back through today) set the range and re-run the report immediately. Board tabs ("All boards" + one per board, local to this page — selecting a board here does not affect the board shown elsewhere in the app) filter results; "All boards" (the default) renders completions grouped by board, Today-view-style — board-ordered, collapsible sections, board-color accents, Expand-all/Collapse-all — while a single selected board renders the original flat list. As of PR #41, this is the **only** place to view completed tasks on the web frontend (the Tasks page no longer has a completed-tasks view).
3. **Settings** (`/settings`) — manage per-user Type labels, configure the High Priority Daily Limit, and manage the account (sign in / sign out). Mode labels were removed in PR #51 (all existing Mode labels deleted from the database; the API now returns 400 for any Mode label operations). The Focused View config editor was removed from this page in PR #41 (backend endpoints remain live; see "Focused View" section).
4. **Task Detail** (`/tasks/:id`, `/tasks/new`) — create or edit a task with full field and label editing, including a board picker (shipped — PR #41; see "Task Form" below)

### Task List / Kanban Board

**Five-view redesign (shipped — web PR #41, Overdue view added PR #55):** The Tasks page header reads "Tasks Are Us - `<View>`" (Overdue/Focused/Today/Tomorrow) or "Tasks Are Us - `<Board name>`" (All). The "tasksAreUs" nav branding and the always-visible board-switcher dropdown were removed from the sidebar/mobile header; board switching is now done via **board tabs** shown only under the All view. The five views are (Overdue shown only when overdue tasks exist):

- **Overdue** (conditional, PR #55) — shows all pending tasks across all boards whose effective date (earliest of `must_do_by`/`target_date`) is strictly before today. Rendered only when at least one overdue task exists in any board. Selected as the default view on page load when overdue tasks exist; otherwise Focused is the default. Positioned first in the pill toggle before Focused.
- **Focused** — the existing high-priority, config-driven Focused View (see "Focused View" section).
- **Today** / **Tomorrow** — the Day View: all pending tasks (any priority) due that single day, across all boards, grouped by board (see "Day View" section).
- **All** — the kanban board described below, scoped to a single board at a time via board tabs.

**Board tabs** appear directly below the view toggle, right-aligned (PR #42), only when All is selected. Selecting a tab switches the shared active-board state used elsewhere in the app (e.g. task-creation defaults). **Board color styling (PR #62)**: the active tab's background is the board's own color (or a palette fallback by position when unset); inactive tabs show a small colored dot in the same color instead of a plain border. **Tab overflow handling (PR #64)**: when board tabs overflow the row width, the earliest tabs (including "All" if present) remain scrollable and reachable — a CSS technique using `min-width: 100%; width: max-content` on an inner flex container wrapped by an `overflow-x-auto` outer container preserves right-alignment when tabs fit within the row, and only starts anchoring/scrolling from the start when tabs overflow. Outside the All view there is no board picker on this page — creating a task instead defaults to the user's default (starred) board (see "Task Form" below). Note: as of PR #42, Settings' Labels editor no longer follows this shared active-board state — it has its own independent board selector (see "Settings Page" below).

**Text search — all views (repositioned and extended PR #56; moved to top of page PR #57)**: A search box (magnifying-glass icon, placeholder "Search tasks…") is the topmost element on the Tasks page, right-aligned, rendered above the page title and view-mode toggle (moved there in PR #57; previously it sat directly above the board-tabs row, and before PR #56 it appeared in the header row and only in the All view). Because it depends only on local input state, it renders unconditionally and stays visible even while the page's one-time overdue check is still in flight. It filters tasks by title and notes (case-insensitive) in all five views: Overdue, Focused, Today, Tomorrow, and All. In Overdue/Focused/Today/Tomorrow, boards with no matching tasks are dropped from the board-grouped card list entirely; if the search excludes every task, a "No tasks match this search" message replaces the board list (and the Collapse all/Expand all toggle is hidden alongside it, since there is nothing left to collapse). In the All view, search continues to compose with the label filter chips via AND semantics.

**No completed-task browsing on this page:** The "Show done" / pending toggle was removed entirely in PR #41. Once a task is completed it is no longer visible anywhere on the web Tasks page in any of the four views — completing a task removes it from view for the remainder of the session with no way to see it again except via the Archive page. This was an explicit, user-confirmed product decision (not a gap): the "Show done/pending" removal and "no replacement view for completed tasks in this PR" were called out as an accepted requirement during planning (`development-plans/PLAN-feat-tasks-view-redesign-web.md`) and were not raised as a concern in either round of plan review.

**All view (kanban board):**
- **Empty board state (PR #70)**: When a board has zero pending tasks (genuinely empty, not filtered to zero via search or tag filters), a centered "New Task" button appears in the main content area instead of the kanban grid. Clicking it creates a new task on that board. Once at least one task exists, the view automatically reverts to the normal kanban grid. When a board is filtered to zero results via search or tag selection, the standard empty state message ("No tasks match this filter") is shown instead of the New Task button.
- Pending tasks are shown in a **6+ column kanban board**: Overdue | Today | Tomorrow | Day After Tomorrow | [Monday (if Friday)] | Upcoming | No Date. Tasks whose effective date is in the past appear in the leftmost **Overdue** column (red accent), not in Today. The **Overdue** column is hidden entirely when it contains no tasks.
- **Column headers with date/day display (PR #54; two-line abbreviated-weekday layout PR #57)**: Date columns (Today, Tomorrow, Day After Tomorrow, Monday if Friday) show the calendar date and day-of-week on their own line below the column name, using an abbreviated weekday (e.g. "Tomorrow" on one line, "July 30, Thu" beneath it — previously a single line with the full weekday name, e.g. "Today (July 27, Monday)"). Overdue, Upcoming, and No Date columns show static titles with no date line. A timezone bug where Day After Tomorrow displayed the same date as Tomorrow, and the Friday-only Monday column displayed Sunday's date instead of Monday's, was fixed in PR #56 — both continue to show the correct date.
- **High-priority/medium-priority/normal-priority split with independently collapsible zones (PR #54, expanded to 3 tiers PR #73)**: **Overdue**, **Today**, **Tomorrow**, **Day After Tomorrow**, and **Monday** (if today is Friday) columns each split into three independently collapsible zones: High (orange, capped), Medium (blue, uncapped), and Normal (gray, uncapped). Each zone has its own collapsible header showing the tier name and a live count of tasks in that zone. Collapsing a zone hides its tasks but the header remains visible; the header itself is a valid drop target for its tier (fixing a pre-existing bug where dropping on a collapsed header fell through to Normal). The high-priority cap (from user settings, default 3) does not apply within Overdue — tasks carry their existing priority; the cap re-engages when a task is dragged to Today, Tomorrow, Day After Tomorrow, or Monday. Only the High zone is subject to the daily limit; Medium and Normal zones have no cap. Dropping a task *into* the Overdue column is blocked. Each date column enforces the high-priority limit independently — the same `high_priority_daily_limit` setting applies per day, not globally across all columns.
- **Collapsible priority zones (PR #54; expanded to 3 independently-collapsible tiers PR #73; drag-over visual feedback PR #77)**: Each of the three priority zones (High, Medium, Normal) in Today, Tomorrow, Day After Tomorrow, and Monday columns displays its own collapsible header. Each header includes a triangle icon, a tier name (High/Medium/Normal), and a live count of tasks in that zone (e.g., "High (3)", "Medium (0)", "Normal (2)"). Clicking anywhere on the header strip toggles collapse/expand — the icon remains a real `<button>` for keyboard users, so Tab+Enter/Space still works. When a zone is collapsed, its tasks are hidden but the header remains visible as a summary; all zones can be independently collapsed or expanded. Collapse state is held in a `ColumnPriorityCollapseContext` mounted above the router (mirroring the Focused View's board-collapse pattern) — it survives in-app navigation (e.g. editing a task and returning) but is still session-only and resets to expanded on a full page reload. Each column's per-tier collapse state is independent. The headers are color-coded (High = orange, Medium = blue, Normal = gray) and each header carries its own `onDragOver` handler so a collapsed zone remains a correct drop target for its tier. **Drag-over visual feedback (PR #77)**: When dragging a task over a collapsed header, the header shows a fixed-color indigo ring (`ring-2 ring-inset ring-indigo-400`) plus the zone-over background tint, clearly indicating the header is a valid drop target regardless of the zone's base color (High and Medium's same-hue light/dark pastel swap is too subtle to read as a highlight on its own).
- **Monday column (if Friday, PR #54)**: When today is Friday, an additional **Monday** column appears after Day After Tomorrow (displaying "Monday (July 31, Monday)" with the actual date and day). The Monday column supports the same high-priority/normal-priority split and collapsible header as other date columns. It has its own independent high-priority limit. The column disappears immediately after Friday ends (no manual dismissal needed); tasks already assigned to Monday remain on Monday in the backend.
- **Drag-and-drop** between columns always updates `target_date` only via `PUT /tasks/:id` — `must_do_by` is never modified by a drag. Dragging to Day After Tomorrow sets `target_date` to today+2; dragging to Monday sets `target_date` to Monday (when today is Friday); dragging to No Date clears `target_date` only (preserving `must_do_by`). **Dropping into the Overdue or Upcoming columns is blocked** — both are read-only drop targets (Upcoming shows a "Read-only" placeholder; no highlight appears on hover). Dragging into the High zone sets `priority = 'high'`; into the Medium zone, it sets `priority = 'medium'`; into the Normal zone or Day After Tomorrow/Monday/No Date, it resets to `'normal'`. The frontend enforces the user-configured high-priority limit (capping High tasks only) before making the API call; the backend enforces it independently. When using the card's up/down chevron stepper to shift priority in a column that doesn't support the target tier (e.g., attempting to shift up to Medium in a non-priority-eligible column), the stepper clamps or demotes the result to Normal instead.
- **Drag-to-reorder within a column (shipped — PR #61)**: Dropping a dragged task above or below another task within the same priority zone of Today, Tomorrow, Day After Tomorrow, Monday, or No Date reorders it to that position (via `PUT /tasks/:id`, using a client-computed fractional value — no server-side renumbering). Within each of these columns, tasks are ordered by `sort_order` ascending (high-priority tasks still list before normal-priority ones); the read-only Overdue column continues to order by effective date (most urgent first), and the read-only Upcoming column orders by `target_date` ascending. A task's position automatically resets to the bottom of its new zone whenever its effective date or board changes — via a drag to a different column, an edit on the Task Form, or a board move — unless the drag itself explicitly set a new position. Reordering is web-only; the mobile app reflects this order but has no drag-to-reorder UI of its own.
- **Label filter chips** are shown above the board (Type only — Mode removed as of PR #51, Frequency hidden as of PR #29), only in the All view and single-board views. The row no longer shows a leading category label (e.g. "Tags") above the chips (dropped PR #57). Filter state persists across navigation via React context. The filter row is split into two groups (updated PR #79, sorting updated PR #81): **Single/And/Or mode toggle + Clear filters** (left, non-scrolling control group) → **tag chips** (right, horizontally scrollable on overflow). **Chip sorting and alignment (PR #81)**: Tag chips now sort with selected tags first (left side of the row), followed by unselected tags, with alphabetical sorting applied within each group. The row is left-aligned so the selected (prioritized) tags lead visually. A reserved scrollbar space (`pb-2 -mb-2` padding) prevents the horizontal scrollbar from overlapping and clipping chip buttons; a thin scrollbar style (`.scrollbar-thin-x`) makes the scrollbar less visually intrusive. **Single/And/Or mode toggle and per-board persistence (PR #75)**: The mode control is a 3-button group visible at all times (Single | And | Or), always-visible with a "Clear filters" link/button grouped with it; the "Clear filters" button is always visible but disabled/greyed when nothing is selected. **Single mode** (the new default) — clicking a tag replaces the current selection with just that tag; clicking the same tag again deselects it (at most one tag selected at a time). **And mode** — match all selected tags (existing behavior). **Or mode** — match any selected tag (existing behavior). The mode immediately re-filters the task list on toggle without requiring any other action. **Per-board tag selection persistence (PR #75)**: Which specific tags are checked for a given board is now remembered for the lifetime of the browser session, keyed purely by `board_id`, regardless of which view or board-tab you're looking at it from. This applies to both places a board's tags can be filtered: the All view's board-tab switching and the pinned/single-board views. The remembered selection is shared by `board_id` only — not scoped per view/viewKey. A page reload loses the session-scoped memory (the mode also resets to SINGLE on reload). The mode (Single/And/Or) itself is a session-scoped shared value across all views — not yet persisted to the database; that's an explicitly deferred follow-up.
- **Text search** — see "Text search — all views" above (PR #56 extended it beyond the All view). Within this view, search composes with the label filter chips via AND semantics.
- When navigating to New Task while a label filter is active, those labels are pre-checked in the form.
- **Task cards** show: title, `must_do_by` date in red with "Overdue ·" prefix when past, `target_date` in gray (hidden when equal to `must_do_by`), label badges for Type labels only (Mode removed as of PR #51, Frequency removed from UI as of PR #29), and up to 3 links (shipped — PR #39; shown as their description — hovering reveals the full URL via a native tooltip, clicking opens it in a new tab). **Card background by column (PR #81)**: Card background color is determined by the task's column, not its priority tier alone. Overdue-column tasks render `bg-red-50` regardless of priority (making overdue status visually distinct at the card level); Upcoming and No Date columns render plain white (these columns cannot hold assigned priority tiers, so no tier-colored tint); all other date columns (Today, Tomorrow, Day After Tomorrow, Monday) render tier-colored backgrounds — light orange for High, light blue for Medium, light green for Normal (shipped — PR #79). The background always falls back to white while a card is in inline quick-edit mode, reverting when editing closes. **Priority stepper (updated PR #78)**: Up/down chevron buttons replace the old 3-state click-to-cycle star button, enabling more flexible priority control. The stepper behavior depends on the current priority: Normal tier shows single/double-up chevrons (→Medium/→High); Medium tier shows single-up/single-down (→High/→Normal); High tier shows single/double-down (→Medium/→Normal). On ineligible-date columns (Overdue, Upcoming), the stepper is hidden entirely. Upward shifts check date eligibility and demote to Normal if the target tier is ineligible; downward shifts never check (you already held an equal-or-higher tier, so stepping down never newly requests an elevated tier). The High tier is subject to the daily cap; attempting to exceed it shows an inline error and reverts the attempt. **Inline date editing (added PR #76)**: The `must_do_by` and `target_date` text on cards are clickable links (styled with underline on hover). Clicking either date opens a native date picker that immediately commits the change on selection via `PUT /tasks/{id}`; no separate Save button. Pressing Escape or losing focus without picking a new date reverts to link display with no network call. **Link text wrapping (PR #70)**: link descriptions on task cards now wrap across multiple lines to fill the available card width, instead of being truncated with an ellipsis at a fixed narrower width. **Label order**: labels on kanban cards are now sorted alphabetically by value (added PR #70), matching the convention already established in the Archive view and filter chips. **Board-color left border (PR #62)**: each card shows a 4px colored left border in the active board's color (falling back to a palette color by position when unset), matching the treatment `FocusedTaskCard` already uses in the Focused/Today/Tomorrow views. The label-badge row reserves a fixed two-row height (PR #57), regardless of how many labels a task has (including zero), so kanban columns line up consistently when switching boards via the board tabs, even though different boards' tasks carry different numbers of tags. Inline edit (title + labels) via a shared `TaskQuickEdit` component (also used by Focused/Today/Tomorrow cards as of PR #42) — Type labels are listed alphabetically in this quick-edit form (PR #57) — inline complete, and inline delete are available directly on cards.

### Task Form (Create / Edit)

- Fields: Title (required), Notes, Must do by (date), Target date (date), Priority selector (visible only when at least one date field falls within the priority-eligible window — today, tomorrow, day-after-tomorrow, or Monday when today is Friday, mirroring the kanban board's own eligible columns; extended to include the latter two in PR #60, previously capped at tomorrow). **3-way priority selector (PR #73)**: a segmented control with three buttons (Normal/Medium/High) allows selecting any of the three tiers. When the form's date fields place the task outside the priority-eligible window, the priority selector is hidden and the priority defaults to Normal when submitted. When visible, the currently selected tier is highlighted. The High and Medium buttons are only available/clickable when at least one date field is eligible; otherwise only Normal is selectable. The selected state is color-coded (High = orange background, Medium = blue background, Normal = gray background). Labels, Links (up to 3, shipped — PR #39).
- **Notes — live Markdown preview (shipped — PR #59; scroll sync added PR #80)**: The Notes field renders as a side-by-side pane on wider screens (edit textarea on the left, a live read-only Markdown preview on the right; stacked vertically on narrower screens) — the preview updates on every keystroke. Rendered via `react-markdown` + `remark-gfm` (GitHub-Flavored Markdown: tables, strikethrough, task-list checkboxes, etc.). Task-list checkboxes render as inert (disabled) checkboxes — the preview is read-only and cannot be used to check items off. Links in the rendered preview open in a new tab (`target="_blank" rel="noopener noreferrer"`) so a click never discards an in-progress unsaved form. An italic "Nothing to preview yet" placeholder is shown when Notes is empty; the preview pane scroll-caps on long notes. **Scroll sync (PR #80):** the textarea and preview pane are scroll-synced in both directions — scrolling either pane positions the other proportionally (by scroll percentage), with epsilon-based comparison to prevent mutual-listener feedback loops. `notes` is still stored and submitted as plain trimmed text — Markdown is a purely client-side rendering interpretation with no data model or API changes.
- When the form's date fields place the task outside the priority-eligible window, the priority selector is hidden and defaults to Normal when submitted.
- Both date fields show an **× clear button** when a date is set, allowing one-click removal. In edit mode, clearing a date sends an explicit `null` to the backend so the field is actually cleared (not silently omitted).
- **Links (shipped — PR #39, auto-ready rows and open-in-new-tab PR #52)**: A blank row always appears at the end (while under the 3-link cap), ready for the user to start typing without an explicit "+ Add" step. When the user fills a row, a new blank row auto-appends. Each row has its own remove button. Links with valid URLs show an open-in-new-tab icon to open in a new tab without leaving the form. See "Links" under Task Model for validation rules.
- **Board picker (shipped — PR #41)**: a Board `<select>` dropdown lists all of the user's boards. Creating a task from the Focused/Today/Tomorrow views (where no board tab is visible) defaults the dropdown to the user's default (starred) board; creating from the All view defaults it to the currently selected board tab. Either default can be overridden before saving. Editing an existing task and selecting a different board moves the task (labels are cleared server-side — see "Move Task Between Boards"); an inline warning ("Moving to a different board will clear this task's labels.") appears once the selection differs from the task's current board. In edit mode, switching the dropdown re-scopes the Labels section to the new board's labels (shown as an inline "Loading labels…" placeholder while they load) without discarding the dropdown selection or any other in-progress field edits — the form stays mounted across the switch (fixed — PR #48; previously the labels refetch briefly unmounted the whole form, reverting the dropdown to the task's original board and wiping unsaved edits).
- **Form layout cleanup (PR #42)**: The "+ Add Link" control now appears immediately after Notes (previously it appeared after Labels), and the Notes textarea is taller. The redundant read-only label chips that used to render below the label picker (duplicating the picker's own selected/highlighted state) were removed.
- **Tags — inline creation (shipped — PR #58)**: The Tags section (the Type category — the only remaining editable label category) shows existing tags as toggleable chips sorted alphabetically by value. A "+ Add" control next to the "Tags" header lets the user create a new Type tag directly from this form (scoped to the form's currently selected board) without a trip to Settings; submitting auto-selects the new tag on the task being created/edited. A duplicate or empty value shows an inline error (e.g. "Label already exists") without closing the panel. The "Tags" header and "+ Add" control render even when the board has zero existing tags — only the chip list itself is empty in that case — since a brand-new board is exactly when inline creation is most needed. The redundant "Labels" caption that previously sat above this section was removed, and the "Tags" sub-header (previously forced uppercase via CSS) now displays as "Tags".
- **Save button state (fixed — PR #48)**: After a successful edit save, "Save Changes" returns to its normal enabled state once the success banner appears, and stays clickable for a subsequent edit (previously it stayed disabled/stuck on "Saving…" indefinitely after every successful edit, even though the save itself succeeded and persisted).
- **Navigation on save (shipped — PR #52)**: After a successful save (create or edit), the page navigates back to the previous location (Tasks view, Archive, Settings, etc.) instead of remaining on the form. There is no transient success banner.
- **Completed task actions (shipped — PR #64 and PR #65)**: When viewing a completed task's detail page (`task.state === 'done'`), two buttons appear: "Mark as Incomplete" and "Delete Task". "Mark as Incomplete" calls the new `POST /tasks/{task_id}/reopen` endpoint, which resets the task to pending state and navigates back to the previous page. The reopen action clears `completed_at`, resets `sort_order` to the default (bottom of the task's new column), leaves `priority` unchanged from its previous value, and bumps `updated_at`. If the task's `must_do_by` or `target_date` place it in the past, it will reappear in the Overdue column on the Tasks page once reopened. "Delete Task" calls `DELETE /tasks/{task_id}` (no state restriction — this endpoint works the same for pending and done tasks) and navigates back to the previous page (added — PR #65).

### Settings Page

- **Boards section (shipped — PR #34, updated PR #37, drag-to-reorder + order-driven default PR #62)**: Appears at the top of Settings, above Labels. Boards are listed in the user's custom order and each row can be dragged (native HTML5 drag-and-drop) to reposition it. Shows: a drag handle; a color swatch (a circle showing the current hex color, or gray if unset — clicking opens a hidden native `<input type="color">`; color auto-saves with a 300ms debounce via `setColorBoard` in `BoardContext`; a `×` button (visible only when a color is set) clears the color); inline rename; a read-only star marker shown only on the topmost board (no longer clickable — the star-click "set as default" action was removed in PR #62; dragging a board to the top of the list is now the only way to change the default); a Delete button (disabled for the default board or the only board — server errors such as "board has tasks" are surfaced inline); and a "+ Add board" button to create a new board. Dropping a dragged board computes its new position via a client-side fractional-index algorithm (mirroring the task drag-to-reorder pattern from PR #61) and persists it via `PUT /boards/{id}` with `sort_order`.
- **Focused View config section — removed from web Settings (PR #41)**: The web Settings editor for Focused View config (board selection, day range) shipped in PR #37 was deleted as part of the Tasks-view redesign, along with the now-unused `getFocusedViewConfig`/`updateFocusedViewConfig` frontend calls. The backend `GET`/`PUT /focused-view/config` endpoints remain live and unchanged — nothing in the web frontend calls them anymore; the mobile Settings screen still has its own config editor (see "Focused View UI — Mobile app"). Users can no longer change their Focused View board-selection/day-range config from the web app.
- **Labels section (shipped — PR #15, updated PR #34, made board-neutral PR #42, updated PR #51, alphabetized PR #62)**: Appears below Boards, above High Priority Limit. Shows a **Type** subsection scoped to a board chosen **independently of the app's globally active board** (Mode labels were removed in PR #51 — all existing Mode labels deleted from database, no longer editable): a dropdown selector appears next to the "Labels" heading whenever the user has more than one board (defaulting to the currently active board on load, and self-healing to the active board or first board if the selected one is deleted), letting the user pick which board's labels to view/edit here without changing the board selected elsewhere in the app (e.g. the Tasks page's board tabs). When the user has only one board, no selector is shown. Type labels are listed alphabetically by value (PR #62 — matching the alphabetical tag sort already used in `TaskForm`/`TaskQuickEdit`/the All-view filter chips). Each Type label has Edit and Delete buttons; the subsection has an Add button to create new values. Labels created here are added to the selected board. Frequency and Mode labels are not shown here (they are not editable). Changes take effect immediately in TaskForm label pickers and on task cards.
- **High Priority Daily Limit**: numeric input (min 1) that sets the per-day cap for high-priority tasks in Today and Tomorrow columns. Defaults to 3 when unset. Reducing the limit below the current count is allowed — a warning is shown but no retroactive removal occurs. Persisted server-side via `user_settings.high_priority_daily_limit`.
- **Connection section (shipped — PR #25)**: Shown at the bottom of Settings (below the Save Settings button). Displays an environment badge — green "Production" when the app is served over HTTPS, yellow "Development" otherwise — and the resolved API base URL. A "Test Connection" button pings `GET /api/v1/health` (unauthenticated) and reports either "Connected Xms" (with measured round-trip latency) or "Failed: <error message>". Implemented on both the web frontend and the mobile app.

---

## Archive

**Renamed from "Reports" (PR #63)** — the route and screen path changed from `/reports` to `/archive` on both web and mobile (the web app's old `/reports` URL redirects to `/archive`). Users can view a history of completed tasks over a user-selected date range. The Archive page shows a date-range picker (From / To) plus three quick presets — **This month**, **Last month**, **Last three months** (a rolling window: the 1st of the month two months back through today) — a total completions count, and a list of completed tasks each with their completion timestamp and labels. Recurring task completions are individually recorded so history is accurate.

**Board filtering (PR #63)**: Board tabs — "All boards" plus one tab per board — let the user scope the report to a single board or every board at once. This board selection is local to the Archive page; it does not change the board shown elsewhere in the app (e.g. the All-view kanban board). "All boards" (the default) renders completed tasks grouped by board, Today-view-style: board-ordered (the user's custom drag-to-reorder order), collapsible per-board sections with an Expand-all/Collapse-all control, and board-color dots/accents on each group and task card. Selecting a single board renders the pre-existing flat list. **Tab overflow handling (PR #64)**: when board tabs overflow the row width, they remain scrollable and reachable using the same CSS technique as the main Tasks page's board tabs (see Board tabs overflow section above).

**Completion card interaction (PR #64)**: Completion cards (both in the grouped "All boards" view and the flat single-board list) are clickable and navigate to the completed task's detail page at `/tasks/:id`, allowing the user to view, edit, or reopen the task. A hover effect (slight shadow) indicates the cards are interactive.

**Alphabetical tag sorting (PR #64)**: Tags on completion cards are sorted alphabetically by value in both the grouped and flat-list views, matching the alphabetical tag ordering convention used elsewhere in the app (e.g. task cards, form label sections).

---

## Authentication

Firebase Authentication is the only identity mechanism. All device-based auth infrastructure (`device_uuid`, `POST /users`, `POST /users/migrate`) has been removed. The backend auto-creates a user row on first authenticated request.

- **Phase 1 (shipped — PR #12)**: Backend only. The backend accepts `Authorization: Bearer <firebase_id_token>` on all endpoints. Three new columns added to `users`: `firebase_uid` (unique, indexed), `email`, `display_name`. The `auth_provider` column is now populated with the Firebase sign-in provider string.
- **Phase 2 (shipped — PR #13)**: Frontend login UI using Firebase JS SDK. The browser sends `Authorization: Bearer <firebase_id_token>`. Key behaviours:
  - **Anonymous sign-in**: On first load the app calls `signInAnonymously()` automatically — zero friction, same UX as before.
  - **Named sign-in**: Users can upgrade from anonymous to a named account via Google (OAuth popup) or email magic link. Named sign-in is only exposed when the user explicitly signs out; anonymous is never a user-facing choice.
  - **LoginPage**: Shown only after an explicit sign-out; offers Google and magic-link options.
  - **Settings → Account section**: Anonymous users see upgrade options (Google / email); named users see their display name / email and a Sign out button.
  - **`client.ts`**: Calls `auth.currentUser?.getIdToken()` asynchronously; attaches `Authorization: Bearer` header on every request.
- **Phase 3 (shipped)**: Removed legacy `X-User-ID` fallback path from the backend; removed `useUser.ts` shim. Bearer token is now the only accepted auth path.
- **Phase 4 (shipped — PR #17)**: Removed all device-based identity infrastructure. Dropped `device_uuid` column from `users` table. Deleted `POST /users` (device registration) and `POST /users/migrate` endpoints, along with `UserCreate`, `MigrateRequest`, and `MigrateOut` schemas, `frontend/src/api/users.ts`, and historical migration scripts (`migrate_user_data.py`, `recover_data.py`). Firebase auto-create in `get_current_user` is now the sole user-creation path.

Sign-in providers supported: Google (OAuth popup) and email magic link. Apple sign-in may be added via Firebase in a future phase (not yet configured).

---

---

## Mobile App

A React Native + Expo managed-workflow client (`mobile/`) ships alongside the web frontend. Both clients share the same `/api/v1` backend; there is no mobile-specific API surface.

### Tech Stack
- **Framework**: React Native with Expo SDK 54 (managed workflow — no custom dev client required for development)
- **Language**: TypeScript
- **Styling**: NativeWind v4 (Tailwind CSS for React Native), configured via Metro and Babel
- **Navigation**: React Navigation v7 — bottom tab navigator
- **Auth**: Firebase JS SDK v10 with `@react-native-async-storage/async-storage` for persistence (compatible with Expo Go)
- **Google OAuth**: `expo-auth-session` (browser-based redirect flow, not a native popup — works in Expo Go)
- **Deep-link scheme**: `tasksareus://` for magic link sign-in in standalone builds

### Navigation / Tabs
The mobile app has three bottom tabs, mirroring the web app's primary pages:

| Tab | Equivalent web route | Status |
|-----|----------------------|--------|
| Tasks | `/` | **Shipped — PR #20** |
| Archive | `/archive` | **Shipped — PR #24; renamed from "Reports" — PR #63** |
| Settings | `/settings` | **Shipped — PR #23** |

**Chat tab removed (PR #46)**: The Chat tab was removed from mobile as part of the Tasks-view redesign. The backend conversational endpoints remain live. A future PR is expected to rebuild the mobile Chat UI (the web Chat UI was similarly removed in PR #41 pending a rebuild).

### Authentication (shipped — PR #18)
Mobile auth follows the same Firebase-only identity model as the web frontend, with mobile-specific implementation details:

- **Anonymous auto-sign-in**: On first launch (or after any non-deliberate sign-out) the app calls `signInAnonymously()` automatically — zero friction.
- **Google Sign-In**: Uses `expo-auth-session` browser redirect flow. In Expo Go, the Expo Go redirect URI (`exp://[local-ip]:8081`) must be added to Google Cloud Console → Authorized redirect URIs.
- **Magic link**: `sendSignInLinkToEmail` with `handleCodeInApp: true`. The email link carries the `tasksareus://` deep-link URL. **Known limitation**: in Expo Go, the email link opens in the system browser and cannot redirect back into the app; the UI notes this explicitly. Full magic-link sign-in (auto-redirect) works only in standalone builds.
- **Cross-device magic link**: If the link is opened on a different device than where it was requested, the app prompts the user to confirm their email address before completing sign-in.
- **LoginScreen**: Shown only after a deliberate sign-out; offers Google and magic-link options.
- **SettingsScreen → Account section**: Shows "Anonymous user" or the signed-in user's display name / email, plus a Sign out button. Anonymous users are offered Google sign-in and email sign-in options to upgrade to a named account. Full label management, High Priority Limit, and starter questions configuration are now also available (shipped — PR #23).
- **Deliberate sign-out flag**: A module-level boolean (resets on app force-quit) prevents the auto-anonymous-sign-in from firing after an explicit sign-out.
- **API client** (`mobile/src/api/client.ts`): Calls `auth.currentUser?.getIdToken()` asynchronously; attaches `Authorization: Bearer <token>` on every request to `EXPO_PUBLIC_API_URL/api/v1`.

### Tasks Screen (shipped — PR #20, updated PR #21, updated PR #26, updated PR #27, updated PR #46, Overdue view added PR #55, header reflow PR #56)

The Tasks tab supports five session-only views (Overdue, Focused, Today, Tomorrow, All), selectable via a pill toggle in the screen header. The Overdue pill is rendered only when overdue tasks exist; otherwise four views are shown. The header reads "Tasks Are Us - `<View>`" (Overdue/Focused/Today/Tomorrow) or "Tasks Are Us - `<Board>`" (All). Overdue is the default view on page load when overdue tasks exist; otherwise Focused is the default.

**All view (kanban board)**:
The All view is a `SectionList` grouped by date proximity:

- **Grouped sections**: Overdue (red header) → Today → Tomorrow → Day After Tomorrow → Upcoming → No Date; empty sections are hidden.
- **Task order within sections (shipped — PR #61, PR #74)**: Within Today, Tomorrow, Day After Tomorrow, and No Date sections, tasks are ordered by their 3-tier priority rank (High < Medium < Normal) then `sort_order` ascending (the same drag-to-reorder position set via the web All view's kanban board — mobile has no drag-to-reorder UI and never writes `sort_order`; it only reflects whatever order was last set via the web app or the one-time `created_at` backfill). The Overdue section keeps a simpler high-vs-not-high split with `updated_at` descending tiebreak (mirroring the backend's Overdue behavior); Upcoming is ordered by `target_date` ascending.
- **Collapse / expand sections (shipped — PR #26)**: Each section header is tappable to collapse or expand it. Today and Overdue start expanded on first load; all other sections start collapsed. When collapsed, the task count is shown in parentheses after the section title (e.g. "Tomorrow (3)"). An "Expand" / "Collapse" text button in the screen header toggles all sections at once (visible only in All view).
- **Board tabs (shipped — PR #46)**: Board tabs appear directly below the view toggle, visible only when All view is selected. Selecting a tab switches the shared active-board state. Creating a task while in All view defaults the board picker to the currently selected tab.
- **Task row** (updated PR #74): title, effective-date chip, priority-tier color bar (High = amber, Medium = blue, Normal = none) + clickable priority-cycle star (High = filled amber star, Medium = partially-filled blue star, Normal = unfilled gray star), label badges (type=purple — frequency badges removed from UI as of PR #29; mode labels removed as of PR #51), up to 3 link chips (shipped — PR #39; shown as their description — tapping a chip opens the URL directly, no hover step on touch), and an Edit button (added PR #46) for parity with task cards in other views. Tapping a task row or the Edit button opens it in the task form in edit mode; tapping a link chip opens the link instead. **Tap-to-cycle priority toggle (PR #74, gated to eligible columns)**: Tapping the priority star in today/tomorrow/day-after-tomorrow/no-date sections cycles the task through Normal→Medium→High→Normal; in Overdue and Upcoming sections the star is static (read-only). Before cycling to High, a client-side `canAddHighPriority` cap pre-check fires against the day's high-priority count; if the cap is reached, an Alert is shown and the cycle is suppressed (the backend enforces the same cap independently on `PUT /tasks/{id}`).
- **Complete/Delete actions (shipped — PR #46)**: Inline Complete and Delete buttons are available on all task rows.
- **Filter panel (shipped — PR #26, updated PR #46)**: A `☰` icon in the screen header opens a collapsible filter panel. The icon turns indigo when any filter is active (visible only in All view). The panel provides:
  - **Text search**: filters tasks by title and notes simultaneously (case-insensitive, live filtering) — same AND semantics as the web client.
  - **Label chips**: Type only (Mode removed as of PR #51, Frequency removed as of PR #29); tap to toggle; same color as task badges (purple); multiple selections compose with AND logic.
  - **"Clear filters" link**: visible when any filter is active; resets text search and label selection.
- **Drag-and-drop between sections (shipped — PR #27, updated PR #28, PR #74)**: Long-press (500ms) any pending task to enter drag mode. A ghost card lifts and follows the finger. Section headers highlight in indigo as valid drop targets. On drop, sets `target_date` only — matching web behavior; `must_do_by` is never modified by drag. Drop targets and their resulting `target_date` values: Today → today, Tomorrow → tomorrow, Day After Tomorrow → today+2, No Date → null. **Dropping onto the Overdue or Upcoming sections is silently rejected** — neither section highlights during a drag; the task stays put. Scroll is disabled while a drag is active. The high-priority limit (from user settings) is enforced for Today/Tomorrow drops of already-high-priority tasks — an Alert is shown and the drop is cancelled if the limit would be exceeded; Medium and Normal have no cap (locked-in product decision). The optimistic update reverts on API error. The list auto-scrolls when the finger is within 80px of the top or bottom edge. **Task ordering within sections (updated PR #74)**: Within Today, Tomorrow, Day After Tomorrow, and No Date sections, tasks are sorted by their 3-tier priority rank (High < Medium < Normal — via a `TIER_RANK` constant mapping each tier to a number) ascending, then by `sort_order` ascending within a tier (the same manual position set by drag-to-reorder on the web All view's kanban board, or the one-time `created_at` backfill for tasks that predate PR #61). The Overdue section keeps a simpler high-vs-not-high split with `updated_at` descending tiebreak, mirroring the backend's behavior (Overdue tasks are not reorderable). Upcoming remains ordered by `target_date` ascending.

**Overdue, Focused, Today, Tomorrow views**:
These views show day-specific and priority-specific tasks. Tasks are grouped by board and displayed as cards with inline quick-edit capability:

- **Overdue view (PR #55)**: Shows all pending tasks across all boards whose effective date (earliest of `must_do_by`/`target_date`) is strictly before today, grouped by board. Rendered only when at least one overdue task exists. Uses the shared `DayView` component.
- **Focused view**: Shows only high-priority tasks from all boards for the configured day range. See "Focused View UI — Mobile app" section above for full detail.
- **Today / Tomorrow views (shipped — PR #46)**: Show all pending tasks (any priority) across all boards for that single calendar day, grouped by board. Same as Focused view but without the priority filter. Uses the shared `DayView` component.
- **Board-grouped rendering (shipped — PR #46, ordering updated PR #61, PR #74)**: Tasks are grouped by board, each group headed by a color-coded board name and task count. Within each board, Focused/Today/Tomorrow tasks are sorted by their 3-tier priority rank (High < Medium < Normal) then `sort_order` ascending (the same drag-to-reorder position set on the web All view's kanban board); mobile has no reorder UI and never writes `sort_order`. The Overdue view keeps the original high-vs-not-high split with `updated_at` descending tiebreak, mirroring the backend's distinct Overdue-vs-Day-View ordering.
- **Task cards (shipped — PR #46)**: Compact cards with stacked layout (title, effective date, labels, links, actions below). Inline quick-edit is available via a shared `TaskQuickEdit` component (same as web). Complete and Delete buttons (added PR #45 on web, PR #46 on mobile) are available directly on cards. Tapping anywhere on a non-editing card still opens the full `TaskFormScreen` modal (unless the card is in inline-edit mode, which suppresses the tap).
- **Loading/error/empty states**: Loading spinner while fetching; inline error banner with Retry button on failure; empty state with Refresh button when no qualifying tasks exist.
- **No completed-task browsing**: Completed tasks are never shown on the Tasks screen in any view; the "Show completed" toggle removed entirely in PR #46. Once a task is completed it is no longer visible except via the Archive page.

**Common header and controls:**
- **Two-row header (updated PR #56)**: The header is split into two rows to give the view-mode pills room to breathe. Row 1 holds the screen title and the "+" create button. Row 2 (directly below) holds the view-mode pill toggle on the left, and — All view only — the Collapse/Expand button and the filter toggle (☰) grouped on the right. Previously all of these shared a single row, which pushed the rightmost pill (e.g. "All") off-screen once a fifth pill (Overdue) was added. Board tabs continue to render in their own row below, unaffected by this change.
- **"+" button**: tapping the indigo "+" button in the Tasks header opens the task form in create mode. When one or more label filters are active (All view only), those labels are pre-selected in the new task form (shipped — PR #28). Edit mode is unaffected — the form always reflects the task's own labels when editing. When creating a task from Focused/Today/Tomorrow views (no board tabs), the form defaults to the user's default (starred) board; when creating from All view, it defaults to the currently selected board tab.
- **Pull-to-refresh**: `RefreshControl` wired to the current view's fetch mode.
- **Focus refresh**: `useFocusEffect` re-fetches data on every tab focus (stays fresh after web-app edits and Settings label changes).
- **Empty state**: "No pending tasks" when no tasks exist; "No tasks match your filters" when filters are active and yield no results (All view only).

New files shipped in PR #20:
- `mobile/src/utils/taskGrouping.ts` — pure `groupTasksForList(tasks, referenceDate?)` → `SectionList` sections
- `mobile/src/__tests__/taskGrouping.test.ts` — 11 unit tests covering all section keys and edge cases

### Task Form — Create / Edit (shipped — PR #21, updated PR #46, Markdown preview added PR #59)

`TaskFormScreen` is a slide-up modal (`pageSheet` presentation) used for both creating and editing tasks on mobile. It is surfaced from `TasksScreen` via a `Modal` component — no stack navigator change was needed.

**Fields:**
- **Title** (required) — auto-focuses in create mode
- **Notes** — multiline text input; unconditionally trimmed on save to fix a prior bug where blank notes were silently omitted (PR #46). **Live Markdown preview (shipped — PR #59)**: a read-only preview pane renders below the text input (stacked, not side-by-side — mobile screen width doesn't support a two-pane layout) and updates on every keystroke; scroll-capped for long notes; shows an italic "Nothing to preview yet" placeholder when empty. Rendered via `@believer/react-native-markdown-display` + `markdown-it-task-lists`, with a custom `rules.html_inline` override so GFM task-list checkboxes (which the plugin emits as raw HTML tokens this renderer family doesn't natively understand) render as ☑/☐ text instead of silently vanishing — checkboxes are inert, not tappable. Tapping a link in the preview delegates to the same `openTaskLink` helper the Links section uses. An explicit style map (not NativeWind `className`, which doesn't reach this library's internal render tree) matches the app's indigo-600/gray-700 palette.
- **Links (shipped — PR #39, reordered PR #46, auto-ready rows and open-in-new-tab PR #52)** — up to 3 description + URL row pairs; a blank row always appears at the end (while under the 3-link cap), ready for the user to start typing without an explicit "+ Add" step. When the user fills a row, a new blank row auto-appends. Each row has its own remove button. Links with valid URLs show an open-in-new-tab icon ("↗" text) that opens the URL via the system app without leaving the form. Same validation as web: both fields required per non-blank row, URL must start with `http://` or `https://`. Moved to appear immediately after Notes (previously after Labels). See "Links" under Task Model for full rules.
- **Must do by** — date picker field with × clear button
- **Target date** — date picker field with × clear button
- **Priority** (updated PR #74) — a 3-way segmented selector (Normal / Medium / High buttons) is shown when at least one date falls within the priority-eligible window (overdue, today, tomorrow, or day-after-tomorrow — extended in PR #60; mobile has no Monday column, so the web's Friday-only-Monday case doesn't apply). The selector is hidden and defaults to Normal for dates further out (e.g. Upcoming). When visible, the currently selected tier is highlighted with a background color: High = orange, Medium = blue, Normal = gray. Only Normal is clickable/selectable when no date is set; once a date is added, Medium and High become clickable if the date is eligible. `isFormPriorityEligible` delegates to the same `getColumn`-based window as the web form's equivalent function, checking `col === 'overdue' || isPriorityEligible(col)`; mobile's `isPriorityEligible(columnKey)` returns true for `'today' | 'tomorrow' | 'day_after_tomorrow'` (same as the drag-and-drop enforcement, so the form and the drag-and-drop path agree on which dates allow Medium/High).
- **Board picker (shipped — PR #46)** — a set of board-name pills; tapping one selects that board for the task. When creating a task from Focused/Today/Tomorrow views (no board tabs), the form defaults to the user's default (starred) board; when creating from the All view, it defaults to the currently selected board tab. In edit mode, the form shows the task's current board; selecting a different board displays an inline warning ("Moving to a different board will clear this task's labels.") and clears the selected labels (server-side behavior when a task is moved between boards).
- **Labels** — Type section only (Mode and Frequency sections removed as of PR #51 and PR #29 respectively); tap to toggle; matches web form colour coding (type=purple). Board-scoped — labels are fetched for the selected board and change whenever the board is switched.

**Behaviour:**
- **Create mode** ("New Task" header): opened by the "+" button; title field is auto-focused; saves via `POST /api/v1/tasks` to the selected board.
- **Edit mode** ("Edit Task" header): opened by tapping a task row; form pre-populated with existing field values; saves via `PUT /api/v1/tasks/:id`. Clearing a date field sends an explicit `null` to the backend so the field is actually cleared. Moving a task to a different board updates the `board_id` field and unconditionally clears labels (server-side: labels are resolved against the destination board).
- **Native date picker**: `@react-native-community/datetimepicker` — iOS shows an inline calendar; Android shows a system modal dialog.
- **422 handling**: if the backend returns 422 (high-priority limit reached), a user-friendly `Alert` is shown and the high-priority checkbox is automatically unchecked.
- **Cancel**: dismisses the modal without changes.
- After a successful save, the task list silently re-fetches to reflect the change.
- **Label dependency fix (PR #46)**: Labels are now keyed on the actual selected board ID, not the stale active-board context, ensuring labels are always fetched from the correct board.

New file shipped in PR #21:
- `mobile/src/screens/TaskFormScreen.tsx` — 380-line create/edit form component

**API client improvement (PR #21):** `mobile/src/api/client.ts` now exports an `ApiError` class (extends `Error`) carrying the HTTP `status` code. All `apiFetch` errors are thrown as `ApiError` instances, enabling callers to branch on specific status codes (e.g. 422).

### Settings Screen (shipped — PR #23, updated PR #46)

`SettingsScreen` is a fully scrollable screen replacing the earlier placeholder. It uses `KeyboardAvoidingView` + `ScrollView` to handle on-screen keyboards gracefully.

**Sections:**

- **Boards section (shipped — PR #35)**: Inserted at the top of `SettingsScreen`. See "Board UI — Mobile app" section above for full detail.
- **Labels section (shipped — PR #15, updated PR #34, made board-scoped PR #46, updated PR #51)**: Type subsection only, showing the user's personal label list scoped to a board chosen **independently of the app's globally active board** (Mode labels were removed in PR #51 — all existing Mode labels deleted from database, no longer editable). When the user has more than one board, a dropdown selector appears next to the "Labels" heading, letting the user pick which board's labels to view/edit here without changing the board selected elsewhere in the app (e.g. the Tasks page's board tabs). The selector defaults to the currently active board on load and self-heals to the active board (or first board) if the selected board is deleted. Each Type label has inline rename (tap to edit, submit to save, Cancel to abort) and Delete (with confirmation alert). The Type subsection has an "+ Add" button to create a new label. Frequency and Mode labels are not shown here (they are not editable/immutable). Changes take effect immediately against the live API. Labels created here are added to the selected board.
- **Focused View config section — removed (PR #46)**: The mobile Settings editor for Focused View config (board selection, day range) was removed as part of the Tasks-view redesign. The backend `GET`/`PUT /focused-view/config` endpoints remain live and unchanged.
- **High Priority Daily Limit**: stepper with − and + buttons (clamped to minimum 1) showing the current limit numerically. Persisted server-side via the Save button.
- **Save Settings button**: persists High Priority Limit in a single `PUT /settings` call. Shows a "Settings saved" confirmation banner for 3 seconds on success.
- **Connection section (shipped — PR #25)**: Shown below Labels. Displays an environment badge — green "Production" when `EXPO_PUBLIC_API_URL` starts with `https://`, yellow "Development" otherwise — and the resolved API base URL. A "Test Connection" button pings `GET /api/v1/health` (unauthenticated) and reports either "Connected Xms" or "Failed: <error message>".
- **Account section**: see Authentication section above.

### Archive Screen (shipped as "Reports Screen" — PR #24; renamed + date presets + board grouping — PR #63)

`ArchiveScreen` (renamed from `ReportsScreen` in PR #63) is a fully implemented completions view.

**Behaviour:**
- **Date-range picker**: From and To text inputs accepting ISO `YYYY-MM-DD` format. Defaults to the last 30 days (today−30 through today) on first render (widened from the last 7 days as of PR #63, matching the web default). Uses local-timezone date arithmetic so the defaults are always correct regardless of UTC offset.
- **Date-range presets (PR #63)**: Three preset buttons — **This month**, **Last month**, **Last three months** (a rolling window: the 1st of the month two months back through today) — sit above the From/To inputs. Tapping a preset sets the range and immediately runs the report, even before the first manual Go tap.
- **Board tabs (PR #63)**: "All boards" (default) plus one tab per board, local to this screen — selecting a board here does not change the board shown elsewhere in the app. Once a report has been run at least once, switching boards immediately re-runs it with the new selection; before the first fetch it only changes what the eventual Go/preset tap will query.
- **Go button**: Lazy fetch on first load — no request is made on mount. The user adjusts the date range first, then taps Go to run the report. The button is disabled while a request is in flight or either date field is empty.
- **Pre-fetch state**: An illustrated "Set a date range and tap Go" prompt is shown before the first fetch.
- **Loading spinner**: `ActivityIndicator` is shown while the request is in flight.
- **Summary bar**: After a successful fetch, shows "N completion(s)" (singular/plural) in the header area.
- **Completions list — flat or board-grouped (PR #63)**: When a single board is selected, results render as the original scrollable list of `CompletionRow` cards (task title up to 2 lines, formatted completion timestamp, label badges sorted by Type only — Frequency and Mode removed from sort order and rendering as of PR #29 and PR #51 respectively). When "All boards" is selected (the default), results instead render board-grouped and collapsible — board-ordered sections (screen-local collapse state, an Expand-all/Collapse-all control) with board-color-accented completion cards, boards with zero matching completions omitted — mirroring the Today view's grouped layout.
- **Empty state**: Illustrated "No completions in this range" when there are no results.
- **Dismissable error banner**: Shown inline on network or API failure; user can tap Dismiss to clear it.

**Backend boundary fix (PR #24):** The `GET /reports/completions` endpoint's `to_date` filter was changed from `completed_at <= to_date` to `completed_at < to_date + 1 day`. This ensures that tasks completed at any time on the `to_date` calendar day are included; previously, PostgreSQL's implicit cast of a bare DATE to midnight excluded tasks completed after 00:00 on the `to_date`.

### Utility Code
Pure utility functions (`taskDateUtils`, `taskFilters`, `taskPriority`, `chatUtils`) and TypeScript types are copied from `frontend/src/` rather than extracted into a shared module. This is a deliberate no-monorepo decision — no shared package or workspace linking is used.

### Environment / Config
`mobile/.env.example` documents all required environment variables: Firebase project config, Google OAuth client IDs (web, iOS, Android), and `EXPO_PUBLIC_API_URL`.

**EAS Build (shipped — PR #25):** `mobile/eas.json` configures two EAS build profiles: `development` (dev-client build, reads `EXPO_PUBLIC_API_URL` from `.env`) and `production` (ad-hoc distribution, bakes in `https://tasksareus-production.up.railway.app` as `EXPO_PUBLIC_API_URL` at build time). The production binary installs directly from an EAS build URL — no TestFlight or App Store required.

---

## Out of Scope (initial build)

- Custom label *categories* (the one active category — Type — is fixed for all users; Frequency has been fully removed as of PR #31; Mode has been fully removed as of PR #51)
- Editing or deleting Frequency label values — moot; all frequency data has been removed from the DB (PR #31)
- Trip as a separate entity (trip is a Type label value)
- ~~Native mobile app (iOS/Android)~~ — **now in scope**: React Native + Expo mobile client is being built (PR #18 scaffolded the project; feature screens follow in subsequent PRs)

---

*Last updated by — *Bashful* after PR #81 (fix: overdue card shading and tag-filter scrollbar clipping — task cards' background colors now determined by date column (overdue→red, upcoming/nodate→white, other→tier-colored); selected tag chips sort first (left side) with alphabetical within each group; tag-filter row left-aligned with reserved scrollbar space to prevent clipping; new `taskCardBg()` and `sortLabelsForFilter()` utility helpers; frontend-only, 35/35 new tests passing, manual verification complete); Previous update after PR #79 (feat: priority-shaded task cards, top-right actions, right-aligned tag filters (web only, frontend only) — removed `layout` prop and `'inline'` render branch from TaskCardBody, consolidating TaskCard and FocusedTaskCard onto a shared "stacked" layout; removed "High"/"Medium" text priority badge entirely (both tiers); added background tint by priority tier (light orange for High, light blue for Medium, light green for Normal) via new `utils/priorityColor.ts` with `PRIORITY_CARD_BG` mapping; tint falls back to white while card is in inline quick-edit mode; LabelFilterChips redesigned: mode-toggle (Single/And/Or + Clear filters) moves to left, tag-pills move to right with horizontal scroll on overflow, mirroring BoardTabs pattern; new `frontend/src/__tests__/priorityColor.test.ts` covers tier→class mapping; 14 web frontend files changed, 193/193 Vitest tests passing, manual verification complete); Previous update after PR #74 (feat: mobile 3-tier priority implementation — `priority: PriorityTier` field added to mobile's Task interface and API payloads; TaskFormScreen's checkbox replaced with a 3-way segmented selector (Normal/Medium/High); TaskRow's static priority badge replaced with a live tap-to-cycle toggle on eligible columns (gated to today/tomorrow/day-after-tomorrow); priority color bars now differentiate High (amber) and Medium (blue) vs. Normal (none); TaskGhost shows corresponding tier colors; task ordering within All-view sections reflects full 3-tier rank (High < Medium < Normal) then sort_order, matching web's kanban; Overdue section maintains high-vs-not-high split only; 106/106 mobile unit tests passing); Previous update after PR #70 (fix: board/tag UX polish batch — six frontend-only improvements: (1) empty board shows centered "New Task" button when genuinely empty (not filtered), reverting to kanban grid once a task exists; (2) tag chips and "Clear filters" now render on the same row instead of stacked; (3) browser tab title changed from "frontend" to "Tasks are us"; (4) labels on kanban cards now sorted alphabetically; (5) new AND/OR toggle appears when 2+ tags are selected, allowing mode switch between "match all" (AND) and "match any" (OR) with AND as default, re-filtering immediately on toggle; (6) task-card link descriptions now wrap to full width instead of truncated ellipsis; backend untouched, no API/data-model changes; 153/153 tests passing); Previous update after PR #69 (feat: board pin + single-board tag filter chips — new per-board pin (📌) control in grouped views (Overdue/Focused/Today/Tomorrow) collapsing all other boards and expanding only the pinned one; when exactly one board is visible, label/tag filter chips appear scoped to that board, mirroring the All view's chips but filtering locally so selections never leak to an unrelated board; extracted the inline chip block from TasksPage into a reusable `LabelFilterChips` component; new utility functions `effectiveCollapsed` and `findSingleVisibleBoard` in `boardVisibility.ts` with unit tests; frontend-only, no API/data-model changes); Previous update after PR #67 (chore: remove Beliefs feature and all LLM/Anthropic integration — backend routes, models, schemas, ai_service.py, anthropic SDK dependency, ANTHROPIC_API_KEY/CLAUDE_* config, and database tables dropped via startup migration); Previous update after PR #64 (fix: Archive tab overflow, task-detail nav, reopen completed tasks, alphabetical tags); Previous update after PR #63 (feat: rename Reports to Archive with date presets and board grouping — web route `/reports` → `/archive` and mobile "Reports" tab → "Archive" (old `/reports` URL redirects); three date-range presets (This month, Last month, Last three months — a rolling window from the 1st of the month two months back through today) added alongside the existing manual From/To range on both platforms, built on local-date arithmetic to avoid the UTC-boundary bug class fixed elsewhere in this app; board-tab filtering added ("All boards" default + one tab per board, local state independent of the app's global active board); "All boards" mode renders completions grouped by board Today-view-style — board-ordered, collapsible sections, board-color accents, Expand-all/Collapse-all; backend `GET /reports/completions` gained an additive `all_boards` query param and `boards` response field, fully backward-compatible with existing single-board callers including not-yet-updated mobile clients). Previous update after PR #62 (feat: board colors everywhere, custom board order, order-driven default — new `boards.sort_order` fractional-index column, same client-computed pattern as `tasks.sort_order` (PR #61); `GET /boards` and the Focused/Day View board-grouping queries switched from `is_default DESC`/alphabetical-by-name to custom `sort_order ASC` ordering; `PUT /boards/{id}` no longer accepts `is_default` as a writable field — the default board is now server-derived as whichever board is topmost in the custom order, auto-recomputed on every reorder; web Settings' Boards section gained native HTML5 drag-and-drop reordering (star-click set-default removed, replaced by a read-only star on the topmost board); mobile remains read-only for board order — its now-dead "Default" tap button was removed, and it simply renders boards in whatever order the API returns; board color now drives All-view board-tab styling (active-tab background, inactive-tab dot) and a 4px colored left border on All-view task cards, matching the border treatment `FocusedTaskCard` already had; Settings' Tags list is now sorted alphabetically). Previous update after PR #61 (feat: drag-to-reorder tasks within date columns — new client-computed fractional `sort_order` field on tasks; the web All-view kanban board supports drag-to-reorder within a date-bucketed priority zone; a task's `sort_order` auto-resets to the bottom of its zone whenever its effective date or board changes; mobile picks up the same ordering read-only and never writes `sort_order`; ordering-contract change: the Focused View and Today/Tomorrow (Day View with `overdue=false`) switched their within-board tiebreak from `updated_at` descending to `sort_order` ascending, since those views have no reorder UI of their own but should reflect the same manual order set on the All view — Overdue's tiebreak is unchanged). Previous update after PR #60 (fix: extend high-priority eligibility to Day After Tomorrow and Friday-only Monday — the backend's `_is_hp_eligible_date` check and the web Task Details "High priority" checkbox both used a narrower `<= tomorrow` cutoff than the kanban board's own high-priority-eligible columns, so dragging a task into the Day After Tomorrow or Monday high-priority zone was silently reset server-side on save, and the checkbox never rendered for tasks dated those two days; both now mirror the board's `isHighPriorityEligible`/`getColumn` rules; a follow-up commit extended the same fix to mobile — `mobile/src/utils/taskPriority.ts` now recognizes Day After Tomorrow (and overdue) for its "High priority" checkbox, though mobile still has no Monday grouping/column and its separate drag-and-drop HP-limit check remains Today/Tomorrow-only). Previous update after PR #59 (feat: live Markdown preview for task Notes, web + mobile — side-by-side edit/preview pane on web (`react-markdown` + `remark-gfm`), stacked preview on mobile (`@believer/react-native-markdown-display` + `markdown-it-task-lists`); GFM task-list checkboxes render inert, links open in a new tab/external app; `notes` remains plain stored text — client-side rendering only, no data model or API changes). Previous update after PR #57 (fix: All-view UX polish (web only) — sticky per-column high-priority collapse state via a new `ColumnPriorityCollapseContext` mounted above the router so it survives navigation, live "High Priority (n)" counts, tidied filter-chip row (dropped the "Tags" label, right-aligned "Clear filters"), search box moved to the very top of every Tasks-page view, a reserved 2-row label-badge height on All-view task cards so kanban columns align across boards, two-line abbreviated-weekday column-header date labels, and alphabetized Type tags in the Task quick-edit form). Previous update after PR #56 (feat: extend text search to all five Tasks-page views (web) via a new `filterBoards` helper and repositioned search box, right-align/alphabetize label filter chips (web), reflow the mobile Tasks-screen header into two rows to stop the "All" pill clipping off-screen (mobile only), and fix two high-priority-divider UX issues (triangle icon, whole-strip-clickable) plus two Day-After-Tomorrow/Monday column date-display bugs (web only)). Previous update after PR #55 (feat: add Overdue view — new session-only pill-toggle view positioned before Focused, rendered only when overdue tasks exist, selected as default view when they do; uses `/day-view/tasks?overdue=true` endpoint). Previous update after PR #52 (feat: improve task edit form — nav-back-on-save (web only), auto-ready link rows (web + mobile), open-in-new-tab icons for valid URLs (web + mobile); removed "+ Add link" button in favor of auto-appending blank rows; added comprehensive unit tests for the new `withReadyLinkRow()` function). Previous update after PR #51 (chore: remove Mode label category — startup migration deletes all Mode labels from database, updates PostgreSQL ENUM type to only include 'type', logs completion; API now rejects POST /labels with category=mode or category=frequency with 400; only Type labels are configurable; backend temporarily keeps Mode in CategoryEnum Python enum to avoid ORM crashes). Previous update after PR #50 (chore: removed chat/conversations API end-to-end — deleted `/conversations` router and backend conversational endpoints, `Conversation`/`Message` models, `starter_questions` setting from all platforms, and related DB tables via startup migration). Previous update after PR #48 (web: fixed two Task Detail Form bugs — switching the Board dropdown mid-edit no longer unmounts and resets the form, and "Save Changes" no longer gets stuck on "Saving…" after a successful edit). Previous update after PR #46 (mobile: Tasks-view redesign with 4-way view mode — Focused/Today/Tomorrow/All; Day View UI shipped; Chat tab and Focused View config editor removed from mobile). Previous update after PR #42 (web: quick-edit extended to Focused/Today/Tomorrow cards; Settings' Labels made board-neutral)*
